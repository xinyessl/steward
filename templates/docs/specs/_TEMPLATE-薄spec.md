---
id: <前缀-NN，如 RE-07；对齐所属域>
title: <一句话说清这次改动>
module: <所属域，对齐导读的 module，如 规则引擎>
type: feature
source: <来源：/fix 缺陷 | 需求 | 人工>
guide: <关联导读，如 docs/specs/RE-00-规则引擎导读.md —— 事实源：库表/字段/代码索引/数据流看它>
priority: P0 | P1 | P2
status: draft
owner_human: <验收人>
depends_on: []
---

> **薄 spec**：用于「已有导读打底」的项目做**单次改动 / 修复**。系统全貌、库表详解、代码索引在 `guide:` 指向的导读里；本 spec 只写「这次改什么、怎么算对」——不重复导读内容。
> 开工前先读 `guide:` 那份导读的相关章节（库表 / 字段 / 数据流），**禁止臆造表名列名**；查不到对应表/列 → 标 `NEEDS-HUMAN` 停下问人。

## 1. 改动目标（一句话）
<要做什么、为什么。多条需求先按 CLAUDE.md §4.6 拆开，各写一份薄 spec。>

## 2. 验收标准（AC · Given-When-Then · 每条可自动化判定）
- **AC1** Given <前置状态> When <操作/输入> Then <可观测结果>
- **AC2** …
- **AC-回归** Given <既有正确行为> When <…> Then <不被本次改动破坏>
  （若在修 bug：把踩坑的那个场景固化成一条回归 AC，见 §4.5 例外）

## 3. 数据契约（复用现有表 · 逐列对导读 / 真实库结构核过，禁臆造）
| 表 | 列 | 用途 | 出处 |
|---|---|---|---|
| <table> | <col> | <…> | 导读 §<x> / db-schema |
> 需要**新表 / 改列** → 停，标 `NEEDS-HUMAN`，不擅改库。

## 4. DoD（完成定义）
- [ ] 全部 AC 通过（含回归 AC）
- [ ] 连真库冒烟：<关键返回 / 校验点>（本项目有真实库时必做）
- [ ] 行为/规则/字段有变 → 已回写 `guide:` 导读对应章节（§4.5 spec 同步纪律）
- [ ] 无臆造字段；踩到的非显然坑已回写 `docs/lessons.md`
