# Steward 全局经验库 / 防回归（通用·跨项目·跨人共享）

> **目的：解决过一次的通用问题，别在任何项目再犯。** 这是 **Steward 工具自身沉淀的"踩坑记忆"**——随 steward 仓库走、提交进 git，**跨所有项目、跨所有使用者共享**。
> **只记通用的坑**：换个项目也会犯的那种——分页/搜索/筛选交互是否生效统一、表单校验通用陷阱、某框架/库的通用坑、跨项目的接口/分页约定……
> **项目专属的坑**（特定样式/主题色、本项目字段或模块怪癖）→ 记到**那个项目的 `docs/lessons.md`**，不要记在这里。
> 文件位置：steward 仓库根的 `lessons.md`（环境变量 `$STEWARD_LESSONS` 指向它）。`cat "$STEWARD_LESSONS"` 读、Edit 写回。

## 规矩（dev agent 必须遵守）
1. **开工前**：先扫一遍下面的「✅ 自检清单」+ 与本次改动相关的「📚 教训明细」，避开已知坑。
2. **收尾时**：本次若踩了一个**非显然**的坑（查了一会儿才搞定、或很可能再犯的那种），**追加一条教训**（格式见文末模板），并把其中可复用的检查点**提炼进「✅ 自检清单」**。
3. **判断要不要记**：显然的小改不用记；值得记的是「下次别人/AI 很可能再踩同一个坑」。
4. 验收/复验时，人会顺带看一眼新增的教训对不对——错的就删，好的留。

---

## ✅ 自检清单（每次交付前逐条过）
> 下面是从历次教训提炼出的可复用检查点，是本文件最有价值的部分。开工与收尾各过一遍。

- [ ] 改了列表/表格/卡片样式 → 长文本、空数据、超多列/超多行都看过，没溢出/没错位
- [ ] **量「壳/卡片是否填满、footer 是否贴底」时，fixture 必须复刻完整高度链 + 真实组件残留节点**：只复刻「壳 + 表」会**假绿**——真机上父链有好几层（`#app .main-container` 靠 `#app` 选择器给 flex 骨架、`.app-main` 靠**组件 scoped** 给 flex 列 + `overflow:auto`），少一层就塌。更隐蔽的坑：`append-to-body` 的弹窗（element `el-dialog`）会在**原位留一枚空 wrapper div**（如 `.base-dialog`），当页面根壳是 `display:flex; gap:Npx` 时，**每个弹窗的空壳都是一枚真实 flex 子项、各吃一段 `gap`**，把表格卡片顶得够不到壳底——「弹窗越多、底部留白越大、同类页表现不一致」。根治：给这枚空壳 `display:contents`（退出 flex 流；弹窗本体在 body、`.el-dialog__wrapper` 是 `position:fixed` 不受影响）。验证只能**用真实组件/真实 SFC 起 headless（Vite dev + CDP 量真机 DOM）**：`#app` 必须真实存在（Vue `el:'#app'` 会用根 vnode 替换挂载节点，漏了 `#app` → `#app .xxx` 全不命中，是最容易骗过自己的假红/假绿源）；组件 scoped 样式只作用于组件自己的模板元素，手写 `<section class="app-main">` 拿不到 AppMain 的 scoped 规则。看守测试要么用真实 SFC，要么静态 fixture 里**把那几枚空 wrapper + 壳的 gap 一起复刻**，并做「删掉 fix 会变红」的敏感性自检。
- [ ] 加了筛选/搜索条件 → 确认条件**真传到后端并生效**（不是只改了 UI、后端没接）
- [ ] **从「用 `{ ...form }` 整体提交」的表单里移除一个字段时，必须同时删掉 3 处**：模板的 `el-form-item`、reactive `form` 里的字段声明、`openXxxDialog` 的 `Object.assign(form, {...})` 回填。**只删模板不删字段** → spread 会把该字段（空串/旧值）继续提交给后端；若后端 update 是「`if(dto.getX()!=null) set(...)`」语义，空串 `''` 会**静默覆盖/清空**该列（传 null 才是不更新）。移除全局字段改按别的维度维护时尤其危险（如把全局「集采」下拉迁到分省表后，若 editForm 还留 `procurement:''`，每次编辑基本信息就把该列清空）。（来源：zenlin ZL-303 tdrugprov6 移除编辑弹窗全局集采下拉）
- [ ] 写持久层 → 列名/表名**逐字核对真实库结构**，没臆造（遗留命名常反直觉）
- [ ] 改了某处共享逻辑 → 按影响面把**所有调用方/受影响 spec** 一并过了
- [ ] **解析 `git ls-remote --heads --tags` 输出时，务必丢弃 annotated-tag 的 `^{}` 解引用行**：annotated tag（`git tag -a`）在 `ls-remote` 里会多输出一行 `<sha>\trefs/tags/<Y>^{}`（指向被标注的 commit 对象）。若不过滤，版本清单里会混进 `v1.2.0^{}` 这种脏值（重复 + 带尾巴），排序/展示/匹配全乱。规则：剥 `refs/heads/`|`refs/tags/` 前缀后，**`endsWith('^{}')` 的行直接 continue**；tags 去重 + 数字感知倒序（`localeCompare(numeric:true)`），branches 把默认分支（main>master>develop/dev>release/*）置顶、其余字母序。这条纯字符串解析逻辑抽成纯函数（`parseGitRefs`），用**真实临时 git 仓** `git tag -a` 造 annotated tag 后 `git ls-remote` 喂进去单测——只 mock 造不出 `^{}` 行、抓不到这个坑。（来源：intake PD-01 子系统分支+tag 多选，ls-remote 列可选 refs。）
- [ ] **给「存 JSON 列的实体」加字段，别改 DDL、只需保证读写两端都带上**：MySQL `JSON` 列（如 `projects.subsystems`）存的是整个对象数组，`INSERT ... VALUES(?, JSON.stringify(subsystems))` + 读时 `JSON.parse` 自动带任意新字段——**给对象加 `branches/tags` 这类字段无需 `ALTER TABLE`**。要核的是：① 落库前有归一化（`normRefList`：字符串数组/trim/去重/单元素长度上限/数组长度上限/非数组→[]），别把脏数据/超长写进 JSON；② **未选/空值不落该字段**（附加式），老数据无字段 = 读出来 undefined = 回落旧行为，天然向后兼容；③ 冒烟直查 `SELECT <json列> FROM t` 断言新字段 + 原字段都在（真库能抓「代码里的 JSON 字段名 vs 落库实际」错配）。（来源：intake PD-01，subsystems JSON 加 branches/tags。）
- [ ] 列表/表格页布局先数搜索条件：**少（≤2~3 个）就单行**——查询/重置按钮紧跟搜索框后面，别另起一行
- [ ] **筛选项只有 1 个（简单文本搜索框/单个下拉）→ 不放前置 label**，直接靠 placeholder（label 与 placeholder 重复多余）；**≥2 个同类筛选项才放 label** 便于区分对齐。例外：日期范围筛选始终保留合并 label（如「出院日期」「申请日期」），因范围控件占位不够自解释（见 UI 规范第 9 条）。
- [ ] **派生/聚合数据时，前端分组/筛选依赖的字段必须一并透传**：生成 board.json / 汇总 JSON 这类「派生数据」时，凡是 UI 拿来**分组、筛选、排序**的字段（如 `module`/`tag`/`owner`）都要写进去——漏一个，对应功能会**静默退化**（如"按模块分组"全落到「未标模块」），且常在重新生成/重启后才暴露，排查时容易误判成数据丢了。改派生脚本后，对一遍「源数据有哪些字段 vs 前端实际用到哪些字段」。
- [ ] **同一实体有「多个输出端点/序列化路径」时，后加的字段要回填到每一条，别只补主 DTO builder**：一个实体（如批次/工单/订单）常有多处出口——一个共享的 `toDto()`/`xxxOut()` **和** 若干"手拼返回对象"的端点（如列表视图里 `groups.push({...})` 直接拼字段、不经 DTO builder）。给实体**后加字段**时若只改了共享 DTO builder，那些手拼对象的端点会**漏带该字段** → 数据落了库、但某个视图读不到（表现为"设了看不到"，易误判成没保存）。防复发：加字段后 `grep` 该字段名 + 该实体的加载函数（如 `loadXxx()`），列全部读该实体、构造响应的地方，**逐个端点核对是否透传**（尤其手拼对象的、和 DTO builder 是两套输出形状）；测试对**每个对外出口**都断言含该字段，别只测主端点。（来源：intake FS-05，批次 `scheduleDate` 后加只补了 `batchOut`，`/api/field/batches` 手拼 group 漏带，实施端看不到排期。）
- [ ] **接飞书 wiki 文档**：除了 `docx:document:readonly`，还要开 `wiki:wiki:readonly`（解析知识库节点）+ 把该**知识库共享给应用**；只开 docx 会在 `wiki/v2/spaces/get_node` 阶段报 `99991672` 权限不足。按"文档在哪类容器(云空间文件夹 / 知识库 wiki)"决定开哪套权限 + 共享对应容器。
- [ ] **"可编辑清单/模板 + 每项带完成态(done/完成人/时间)"：编辑器每行必须回传原项 `id`（`按 id 合并保留完成态`），否则每次保存清单都当新项、完成态全丢**。当一份清单（部署清单/实施任务清单/检查表）**既能被运营编辑增删项、又被使用方勾选完成**时，保存走「按 id 合并」（旧项按 id 匹配保留 `done/doneBy/doneAt`，新项 done=false，删项即删）。前端编辑器渲染每行时要把**原项 id 藏进 DOM**（如 `data-id`）、收集时带回；只收集 `{title,desc}` 不带 id → 后端匹配不到旧项 → 全部当新项、已勾完成态被清零（表现为"编辑了下标题，之前勾的全没了"）。新增行 id 留空（后端补）。测试必造：勾若干项 → 编辑清单（改标题/删项/加项）保存 → 断言未删项的 done/完成人/时间**仍在**。（来源：intake FS-06，batches.html 实施任务清单编辑器 `implEditRow(id,title,desc)` + `data-id` round-trip；后端 `mergeImplTasks` 按 id 合并。）
- [ ] **"标准模板 + 每实例 overlay 完成态"进度算法：分母用 live 模板项数、分子只算「指向现存模板项」的完成态**。当一份全局模板（部署清单等）套用到 N 个实例（每院/每单），每实例只存**已完成项**的完成态（overlay `{[taskId]:{done,by,at}}`，不存未完成项）时：① 模板增删项要**对所有实例即时生效**——进度 = `已完成且 taskId∈live模板 的项数 / live模板项数`，**读时派生、不落文件**（别把 total 存进实例，否则模板改了各实例分母不同步）；② 模板删了某项，实例里残留指向它的完成态**既不计入分子也不影响分母**（left join 语义，别报错、别算进 total）；③ 保存实例完成态用 `'xxx' in body` 判存在保留原值（偏更新的接口不带该键时别清空）。测试造：模板 M 项→某实例完成 K 项→删模板 1 项→断言该实例进度随分母缩小、已删项不计。（来源：intake FS-06，`deployProgress(liveTpl, customer.deployTasks)` 读时派生 deployDone/deployTotal。）
- [ ] **"打开菜单"的可点区域别直接绑破坏性动作**：顶栏用户名/头像、行内"更多"这类应该**点了弹下拉菜单**的区域，整块 `@click` 不要直接绑 `logout`/`delete` 等动作——否则点一下就执行（用户以为只是展开菜单）。正确：用下拉容器（如 `el-dropdown`）包住可点块，破坏性动作放进菜单项、由其 command 触发，并加二次确认。多子应用/多页面同种顶栏要**全量对齐**（一个有 bug 往往全有），可拿已正确的那个当范式照抄（见 [[L-006]] pams 顶栏点名字即登出）。
- [ ] **开关类「即选即存」（el-switch 切换即保存生效）**：影响外发/启停等**有副作用**的总开关，别埋在「保存」按钮里（用户切了常忘点保存、以为生效其实没存）。做法：① el-switch 加 `@change`（**注意 `v-model` 已先把绑定值置成新值、@change 才触发**，故"新值"= `v===true`、"原值"= `!新值`）；② 切换先 `ElMessageBox.confirm` **二次确认**（启用/关闭两个方向各一句明确后果提示）；③ 确认 → 立即调保存接口（payload 用"当前已保存配置 + 新开关值"，敏感字段如 apiKey 传掩码/空=不改）→ 成功提示「已…，即时生效」+ 回读确认；④ **取消 / 保存失败 → 还原开关回原值**（别留「切了但没生效」的视觉假象）。**防循环**：el-switch **程序化赋值（回读回写 / 取消还原）默认不触发 @change**，故无「保存→回读→再触发 @change」死循环；仍可加一个 `switching` 标志位 + 还原函数（守卫期间 @change 直接 return）作纵深防御。另一等价写法是改用单向 `:model-value` + `@change`（不用 v-model），从根上不回写、无循环可能（pams datacenter「文件合并规则」启停列即此法）。
- [ ] **登录前要读的「公开配置/品牌」必须单独开免鉴权端点 + 精确白名单，别复用要鉴权的配置接口**：登录页（登录前，无 token）要展示医院 Logo/单位名/主题色等系统配置时，通用配置接口（如 `/config/list`）是**要鉴权的**——直接调会 401、页面空白。做法：① 后端**新开一个只读公开端点**（如 `/auth/brand`），**只返回展示必需的少数字段**（logo/company/name），**绝不透传整份配置**（防敏感项泄露）；② 在鉴权白名单**精确放行该路径**（与 `/login` 并列，别用 `/**` 放太宽）；③ 前端用**原生 fetch 直连**该端点、**绕过**带 Authorization 头 + 失败弹 toast 的 axios 拦截器，任何失败（网络/超时/非 200）**静默回退写死文案**、绝不阻塞登录；④ 配置缺失/JSON 非法时端点返回**空字段**（不报错），前端据空回退。附带图片资源（如 Logo 走 `/file/...`）确认其**由静态服务/nginx 在同源根路径提供、不经业务鉴权拦截器**，则登录前天然可加载、无需额外放行（`/file/**` 不在 `/api/**` 拦截范围内）。多个子应用登录页共用**同一套取数 + 回退**工具，避免各写各样（见 pams UC-01 AC-9 四登录页读 `sys_config.hospital_config`）。
- [ ] **给「已存在」的表加列：`CREATE TABLE IF NOT EXISTS` 不会补列 —— 存量库必须另跑幂等 `ALTER TABLE ... ADD COLUMN`**。手写 SQL 持久层（非 ORM 自动迁移）给旧表加字段时，只改建表语句对**已存在**的表无效（IF NOT EXISTS 整表跳过），新增列只在全新库生效、存量库悄悄缺列 → 运行时 upsert/读取该列报 `Unknown column`。做法：在 init/迁移里加一段幂等补列——先查 `information_schema.COLUMNS`（`TABLE_SCHEMA=DATABASE() AND TABLE_NAME=? AND COLUMN_NAME=?`）判存在再 `ALTER ... ADD COLUMN`（或 try/catch 吞 MySQL 1060 "Duplicate column"），可重复执行不报错。新增列给 `DEFAULT` + 读取侧对存量旧行 NULL 做兜底（如 `enabled: r.x==null?1:...`）。上线时生产库同样要跑这段迁移（应用启动即执行则天然覆盖）。
- [ ] **超大字典（数千项）下拉别全量拉：用 el-select `filterable remote :remote-method` 远程搜索；`allow-create` 允许自定义值时，maxlength 不生效——提交前必须按库列长手动截断**。当一个下拉的候选项有几千条（如 WHO-ART 不良反应术语 6717 条）时，一次性 `GET .../items` 全量下发会拖垮首屏/内存——改成远程搜索：后端给取项接口加**可选** `kw`(itemValue/itemCode 模糊) + `limit`(默认 50、硬上限如 200 防恶意大值)，**不传 kw 时行为完全不变**（返回全部启用项，保护现有小字典下拉向后兼容，别破坏 ROUTE/单位/频次这类）；前端 `remote-method` 里带 300ms 防抖调搜索接口填 options，多行子表**每行独立 options/loading**（共享一份会串扰）。关键坑：`allow-create`（允许填字典外自定义值）下，用户**手打**的自由文本**不受 `<el-input maxlength>` 约束**（el-select 没有等价 maxlength 生效路径）——若该字段对应 DB `varchar(N)`，直接提交超长会**入库报错/被 DB 截断**。做法：提交前对该字段（含每行明细）统一 `v.slice(0,N)` 截断（N=真实列长，先查库核对），别指望 UI 拦。后端若对该字段**不做字典强校验**（自由文本、术语库仅辅助匹配），要确认接口不会因非字典值拒绝提交。测试：连真库 `kw=<真实中文片段>&limit=K` 断言 ≤K 且命中含片段、`kw=<编码片段>` 能按 code 命中、`无 kw` 的小字典条数=旧全量（向后兼容）。（来源：upwd DICT-01/RPT-02，不良反应名称接 ADR_TERM 6717 条远程搜索 + allow-create，adr_name varchar(128) 提交前截断。）
- [ ] **入库前把「中文/布尔/字符串」状态值归一成存储码，别原样入库**：前端筛选下拉/提交可能传中文（"启用"/"停用"）、布尔（true/false）、或 '0'/'1' 字符串；持久层列是 TINYINT/枚举码。写一个归一函数（如 `normEnabled(v,def)` 覆盖 `0/false/'0'/'停用' → 0`、`1/true/'1'/'启用' → 1`、`null → def`），save 入参先过它再落库；**编辑时 payload 未带该字段 → 保留原值**（别默认成 0/清空，像 projects 那样被误抹）。
- [ ] **状态机/枚举加新值：后端"白名单+合法流转+映射"多处联动 + 前端硬编码镜像 一起改，缺一则静默错**。当状态/枚举同时被 ① 后端合法性白名单（`to` 必须 ∈ 它，否则拒）② 合法流转邻接表（`from→to` 不命中则拒）③ 到旧粗粒度/聚合态的映射函数（看板/分组用）④ 前端**各页硬编码的镜像副本**（下拉选项、状态色 class、按钮标签/可选下一步）多处引用时——加一个新值要把这几处**全部**同步。只改后端白名单：漏改流转表→新值无入口全被拒；漏改映射→聚合/看板归错桶；漏改前端镜像→下拉缺项/无色/按钮不出。做法：加值前先列全"引用这枚举的地方"清单（grep 枚举名 + 中文态字面量），逐处改，然后**跑回归测试确认旧链路不坏 + 新值可用 + 非法值仍被拒**。前端若是从后端拉枚举（单一事实源）最好；若各页硬编码副本，务必当"多份镜像"对待。
- [ ] **给「多步编辑器」加未保存拦截（防误返回丢数据）：dirty 跟踪 + 路由守卫 + beforeunload 三件套，且 deep watch 清脏要 nextTick**。给长表单/配置编辑器（维度-子项-选项这类多层可增删的编辑页）加「防误操作丢内容」时：① dirty 跟踪——`watch(编辑数组, deep)` + `watch(关键标量字段)` 任一变化置 `dirty=true`；保存/发布/暂存成功、取消放弃、进入编辑态/载入 都要清 `dirty=false`。②**关键坑：Vue `watch` 默认 flush:'post'（异步，DOM 更新后）**——在「程序性重建编辑数据」（载入/回滚快照/暂存回填）后**同步** `dirty.value=false` 会被随后 flush 的 deep watch **重新置脏**（因为你刚替换了整个数组引用），表现为「刚载入/刚暂存就显示未保存」。正解：用 `suppressDirty` 门 + `nextTick(()=>{dirty=false; suppressDirty=false})`（`markDirty` 里 `if(suppressDirty) return`）兜住这一轮 flush。③ 路由离开守卫 `onBeforeRouteLeave`（从 `vue-router` 具名导入，非全局 guard）→ dirty 时 `ElMessageBox.confirm`，取消返回 `false` 拦住（拦浏览器返回站内路由/侧栏跳转/取消回列表）。④ `beforeunload`：`e.preventDefault()`+`e.returnValue=''` 触发浏览器原生确认（拦关闭/刷新/地址栏跳转/返回站外，**自定义文案现代浏览器忽略**），`onBeforeUnmount` 务必 `removeEventListener`。⑤「暂存」这类**存后留在本页继续改**的操作，落库前对编辑数据**深拷贝**再做 `normalize`（原地 normalize 会污染用户正在编辑的实时数据——如 `delete sub.sources`/清空字段——因为不像「保存后跳转」那样随即离开；navigate-away 的 save 才能原地改）。（来源：zenlin ZL-201 ttplimp19 运营端模板编辑器暂存+离开拦截。）
- [ ] **状态徽章/标签别用「文案」判色，按「状态码/枚举」上色**：用文案正则匹配着色（如 `/已?归档/→绿`）会把语义相反的态误判——「**未归档**」被 `已?归档` 命中判绿、与「已归档」同色，肉眼无法区分。凡有稳定状态码（后端 enum / DB 枚举列）的字段，前端建**独立的「码→{中文,颜色}」映射**（逐字段镜像后端枚举，颜色/中文对齐、别抄错），徽章按**状态码**取色、下拉筛选也用同一份映射生成全量选项；未知/空态兜底中性色 + 显示原值、不报错。**别改通用文案判色函数**（其它页在用，避免回归），新建映射自用。（来源：pams BMS-02 病案状态列，`emr_status` 9 态。）
- [ ] **有「写穿/reconcile」副作用的关联字段（如 A 保存时会重算 B 的绑定），测试 setup 要按写穿口径传全字段，别只单独写 B**：当「A（如客户/组织）保存」会**先清后写**地重算「B（如账号）的某关联数组（负责的医院/项目/角色）」——即 B 的该字段是「以 A 为唯一真源、每次 A 保存都 reconcile」时，测试若**先单独给 B 设值、再保存 A 却不带对应关联参数**，A 的写穿会把 B 刚设的值**清掉**（空参数=解绑），导致后续依赖 B 该值的逻辑（如按 sites 做数据隔离）静默失效、越权数据泄露。做法：① 认清哪个是「唯一真源」——若 B.关联 由 A 写穿维护，测试就该**在 A 保存时带上关联参数**（如 `customer-save {impl:{name:'X'}}` 把医院绑回账号 X），而不是只 `account-save {sites:[...]}`；② 或把「给 B 设关联」的步骤放到「A 保存」**之后**（避免被后来的写穿覆盖）。排查信号：隔离运行某越权/收敛用例失败、且被越权实体的「作用域数组」意外为空 → 十有八九是前面某个「主档保存」把它 reconcile 清了。（来源：intake FS-02/03，`customer-save` 的 `reconcileSiteToImpl` 空 impl.name = 从所有账号解绑该医院。）
- [ ] **导出「可下载文件」端点：`Content-Disposition: attachment` + 中文/非 ASCII 文件名用 RFC 5987 `filename*=UTF-8''<percent-encoded>`（别只写裸中文 `filename="报表.md"`）**。HTTP header 值须 ASCII——直接把中文塞进 `filename="…"` 在部分运行时/反代会抛「Invalid character in header」或落成乱码文件名。正确写法：`'Content-Disposition','attachment; filename="'+encodeURIComponent(name)+'"; filename*=UTF-8\'\''+encodeURIComponent(name)`（同时给 ASCII 兜底 `filename=` 和现代浏览器认的 `filename*=`）。配 `Content-Type` 对（md→`text/markdown; charset=utf-8`、csv→`text/csv`、CSV 中文再加 UTF-8 BOM `﻿` 防 Excel 乱码）+ `Cache-Control: no-store`。**前端触发下载最省事 = `<a href="/api/export?..." download target="_blank">`**（GET 端点、同源自动带 cookie 鉴权），别用 fetch 再手拼 Blob（除非要带自定义 header 或 POST）。测法：`fetch` 该端点断言 `content-type` + `content-disposition` 含 `attachment` + body 内容，不必真触发浏览器下载。（来源：intake BP-01 批次导出开发清单 md。）
- [ ] **动态 innerHTML 里的按钮/表单，绑事件用「事件委托挂容器」或「重渲后即时 addEventListener」，别在元素还没渲染时提前绑**。列表/详情用 `container.innerHTML=模板` 整块重渲后，里面的按钮是**新 DOM 节点**——之前对旧节点的 `addEventListener` 全失效。两种稳妥做法：① **事件委托**：`container.addEventListener('click', e=>{ const b=e.target.closest('#btnX'); if(b) doX(b.dataset.id); })`（容器只绑一次、对后续任意重渲的子按钮都生效）；② **渲染后即时绑**：在写完 innerHTML 的**同一函数末尾** `container.querySelector('#btnX')?.addEventListener(...)`。别在页面初始化时对「还没渲染出来的详情内按钮」`$('#btnX').addEventListener`（此刻是 null 或旧节点）。读表单值同理——重渲后 `$('#inp')?.value` 命中新节点。（来源：intake BP-01 batches.html 详情抽屉动态渲染上传包表单/预览按钮。）
- [ ] **要「连真实数据源冒烟」但整个 server 一启动就连 DB（连不上即退出）时：把「读取器 / 纯 IO 逻辑」抽成独立可 import 的模块，用真实 fixture（临时 git 仓 / 临时文件）单测，绕开 boot server + DB**。当被测能力的**真实数据源不是那个连不上的 DB**（如：清单来自 git tag 里的文件、配置来自本地 JSON、报表来自 CSV）时，别为了测它去 spawn 整个 server——server 常在启动时 `await db.init()` 失败即 `process.exit(1)`（本地无 MySQL/Redis 直接跑不起来），于是真正该测的读取逻辑也一起跑不了。做法：① 把「读数据源 + 解析 + 规范化」抽到一个**不依赖 DB、不 spawn server** 的纯模块（如 `xxx-reader.mjs`），server 只是 `import` 它 + 加缓存/鉴权薄封装；② 测试里**造真实 fixture**——测 git 读取器就 `git init` 临时仓、`commit` 真文件、打真 tag，然后直接 import 读取器断言（不 mock git，抓得到「代码里的路径/tag vs 真实仓」的错配）；测文件存就写真临时 JSON。③ 这样本地 `node --test` 必绿、且是**连真数据结构**的冒烟（比只 mock 强）；另留一份 spawn-server 的集成 smoke 备查、头部注明需 DB。判据：**"真库冒烟"里的"真库"= 该能力的真实数据源，不一定是 MySQL**——数据在 git 里就连 git 仓测，别硬凑 DB。（来源：intake 部署清单「跟随产品代码」重构，`readDeployManifest` 从 git tag 读 `docs/deploy.json`，抽到 `tools/deploy-manifest-reader.mjs` 用真实临时 git 仓测 17/17 绿，无需 MySQL。）
- [ ] **离线校验 Vue SFC（改样式/主题色的纯视觉批改）：用 `vue-template-compiler.parseComponent` 切块，别正则抠 `<template>…</template>`；scss 编译前把 `::v-deep`/`::v-deep()`/`:deep()` 先转成普通选择器**。两个必踩的坑：① 用 `/<template>([\s\S]*?)<\/template>/` 抠模板会被**内层 `<template slot-scope>` 提前截断** → 报一堆"has no matching end tag"假错；正解 `parseComponent(src).template.content` 拿顶层模板、`.styles.filter(s=>s.lang==='scss')` 拿样式块。② Vue2.7 的 `::v-deep() { … }`（空括号包裹式深选择器）是合法 SFC 语法，但 dart-sass 不认——若把它替换成 `& { … }` 会因"顶层不能用父选择器 &"报 `expected "}"` 假错；正解替换成一个**普通类选择器**（如 `.v-deep-wrap`）再喂 `sass.compileString`，只为验证**内部规则**能编译。`@import '@/styles/x'` 记得替换成绝对路径 + `loadPaths:['src/styles']`。判据：模板/样式"编译报错"先怀疑是校验脚本对 SFC 语法的误伤，别急着改源文件。（来源：petl-admin 主题色收尾批改 5 个 SFC 离线校验，正则抠模板被 slot-scope 截断 + `::v-deep()`→`&` 顶层报错，均为脚本假错。）
- [ ] **要「起真 server 打真 HTTP 端到端冒烟」但被测能力不依赖那个连不上的 DB、server 却在 boot `await db.init()` 失败即 exit 时：用 `module.register` loader hook 把 `./db.mjs`（DB 层）替换成 stub，其余全真（真路由 / 真文件存 / 真 git）**。L040 说「抽纯读取器单测」——但当要验证的是**端点级 HTTP 全链路**（鉴权 + 路由 + 文件读写 + 逻辑，不只是读取器）时，抽纯模块不够、又不该为缺 MySQL 就放弃端到端。做法：① 写 `db-stub.mjs`——把 server import 的 DB 模块所有导出实现成 no-op / 返回空（`init`/`ping` 返 true、`loadAll` 从测试 fixture 目录读 accounts/projects 供鉴权、写类方法空实现）；② **Node 22+ 用 `module.register`（不是废弃的 `--loader` 直接导出 resolve）**：`register.mjs` 里 `register('./hooks.mjs', pathToFileURL('./'))`，`hooks.mjs` 导出 `resolve(spec,ctx,next)`——命中 `spec.endsWith('/db.mjs')||spec==='./db.mjs'` 就返回 `{url: STUB, shortCircuit:true}`，否则 `next()`；③ 启动 `node --import ./register.mjs server.mjs`，用独立 `INTAKE_DATA=<临时目录>` + 独立 `PORT` 隔离，造真实 fixture（真 git 仓 + 真 batches.json/customers.json/accounts.json）→ 真 `http.request` 走登录 → 业务端点全链路。这样 MySQL 缺席也能跑**真 HTTP 端到端**（抓路由/鉴权/文件结构/序列化的错配，比读取器单测覆盖更全）；本改动完全不碰 DB 时，stub 掉 DB 不损冒烟真实性。鉴权账号 fixture 的密码要按 server 的散列口径生成（如 scrypt salt+64B）。**⚠️ `register(spec, parentURL)` 第二参是「解析 spec 用的父 URL」——`register.mjs` 里直接传 `import.meta.url`（它本身已是 file URL 字符串），别再 `pathToFileURL(import.meta.url)` 包一层**（会把 file URL 当成路径再拼一次 → `ERR_MODULE_NOT_FOUND: .../file:/.../hooks.mjs`）。等价可传 `pathToFileURL('./')`（目录 URL），但传 `import.meta.url` 最直观、`./hooks.mjs` 相对它解析即同目录。同理 `hooks.mjs` 里算 STUB 绝对路径用 `pathToFileURL(path.join(path.dirname(fileURLToPath(import.meta.url)), 'db-stub.mjs')).href`。（来源：intake 咨询转工单冒烟，误 `pathToFileURL(import.meta.url)` 致路径翻倍；改传 `import.meta.url` 即通，5/5 端到端绿。）
- [ ] **关键词检索选 top-N 段喂 AI/列给用户前的重排：按稀有度（df/IDF）加权，别按「命中词个数/种类数」直接排——否则被大众词泛词大页淹没稀有词真答案**。任何"多词命中 → 取最相关几段"的检索（源码 grep / 文档 / 日志 / RAG 召回都算），有两个隐坑：① **多词切分后不排序就 `slice` 截断词预算** → 大众短词（中文 2-gram）先占满名额，判别性最强的长 n-gram / 英文标识（表名 `x_y`、驼峰方法名、接口路径段）被截掉、根本没参与检索；② **文件/段按「命中的不同词种类数」排** → 蹭 6~8 个大众词的泛词大页 terms.size 高，压过只含 1~2 个稀有词的真答案页。做法：截断词预算前**按特异性降序排**（英文标识 > 长 n-gram > 短 bigram，如 `spec = isIdent ? 100+len : len`）保证判别性词进预算；重排改**稀有词 IDF 式加权**——统计每词文档频率 df（命中它的不同文件数），`weight = lenBonus / log2(2 + df)`（稀有 df 小→权重高、大众趋 0、长词/英文标识加成），文件得分 = 命中各词 weight 之和降序。测试造「频率倾斜」fixture：稀有词文件 df 小 + 一批泛词大页 df 大且 terms.size 高，断言修复后稀有词文件排 top、**反证**旧 terms.size 排序把它挤出（证明 bug 真实）。（来源：intake 深入思考 codeSearch，稀有词「说明书」真答案被泛词大页压掉、AI 只答通用话术。）
- [ ] **多层路由/检索（关键词层 + 精确名反查层 + 兜底层）里，「带人工经确认事实（answerFacts）的语义层」要排在「精确名反查层」之前——否则含精确名的问题被精确层抢走、丢掉那份高价值人工内容**。做「先路由到模块、命中才检索」这类 RAG 前置路由时，常见分层：tier-1 语义/关键词路由（命中会带出**人工整理的经确认事实、易混淆提示**）、tier-3 精确名反查（query 含表名/配置名/接口路径 → 直接映射到所属 spec）。直觉容易让 tier-3 先跑、命中即短路（"精确名最可信"），但精确层只给到"哪个 spec"、**给不出那份人工事实**（如"这个配置在 A 系统维护、不在本地"）。若某问题**既含精确名、又有对应的语义路由条目**（如「order_instruction 怎么配」——`order_instruction` 是配置名、也有一条专门讲它的 questionRoute），tier-3 先命中就把答案降级成"读 spec 章节自己猜"、丢了 answerFacts，AI 反而可能答错。正确顺序：**语义层(tier-1)优先**（过阈值即返回、带 answerFacts/负向提示）→ 没过阈值才 tier-3 精确名兜底 → 再 tier-2 spec 兜底。判据：**哪层携带的信息价值高（人工确认过、含"别混淆/别臆造"），哪层优先**；精确名反查是"tier-1 漏了才补"的增强，不是首选。连真数据冒烟必造「含精确名 + 又有对应语义条目」的问题，断言它走 tier-1 且注入了 answerFacts（不是被 tier-3 抢走）。（来源：intake PD-04 答疑功能模块路由，pwrs 真 map 上 `order_instruction 怎么配` 起初被 tier-3 精确层抢、答案丢了"是 usercenter 配置不在本地"的 answerFacts。）
- [ ] **RAG 前置路由「未命中就明确说没有、别兜底喂全库让 AI 编」——миss 走确定性固定话术、不调模型**。当检索是"喂给 AI 作答的依据"时，旧做法"跨全库关键词召回 top-N 段无脑喂模型"会在**问题跑题/库里根本没有**时，仍召回一堆不相关段落，AI 据此**一本正经地编**（幻觉、答偏）。加确定性前置路由后，**miss（路由到任何模块都没过阈值）应直接返回固定话术**（"该问题在《X》说明书里没找到相关描述，建议转工单/联系开发"）、**根本不调模型**（省钱 + 杜绝据跑题内容编）——而不是"路由没中就回退喂全库"。例外：若有"深入源码"这类独立检索通道，miss 后可让它单独跑（源码命中→用源码答），spec 路由 + 源码都 miss 才固定话术。落地要点：① miss 固定话术分支必须在 `callModelStream` 之前、自身不调模型；② 仍要 SSE 出话术 + 正常 `done` 收尾 + 落库该轮（可追溯"这次是 miss"）；③ 只对"有路由能力（有模块地图/索引）的产品"启用新链路，没有的保持原召回行为不变（向后兼容硬要求 + 测试断言）。测试造：完全无关问题 → 断言路由 miss、不调模型、返回固定话术原文。（来源：intake PD-04，consult 从"specSearch 跨全库召回喂模型"改"路由未命中→固定话术不调模型"。）
- [ ] **列表「主源 + 按共享键的旧数据兜底」时，软删「主源」记录要显式记下它的共享键、让兜底路径跳过——否则删了主源，兜底会按共享键把它「复活」回来**。当一个列表的口径是「新的主实体（如会话记录/主档）为主 + 缺主实体的旧数据按某共享键（sessionId/单号/外键）归组兜底」时：软删某条**主实体**后，若兜底逻辑仍能按那个共享键从**关联子记录**（工单/明细，本就该独立留存）重新聚合出这条 → 表现为「删了却没消失」（换个样子又冒出来）。做法：① 收集**被软删的主实体**的共享键集合（`deletedKeys`，注意软删记录常在数据收集阶段就被 `if(e.deleted)continue` 滤掉，得**单独再扫一遍**拿它们的键）；② 兜底归组循环里 `if(deletedKeys.has(key)) continue` 显式跳过；③ 明确「删主源 ≠ 删它关联的子记录」——子记录（工单/明细）在各自清单里独立留存、独立删。测试造：主实体建了关联子记录 → 软删主实体 → 断言该会话从本列表**彻底消失** + 关联子记录**仍在**它自己的清单里。（来源：intake FS-04 对话记录，删 `intake-conv` 会话记录后被「旧工单按 sessionId 兜底」复活。）
- [ ] **给「日常清单/收件箱」新增一种「非工单」记录类型（会话记录/草稿/日志）时，务必在共享的 `listXxx()` 里排除它——否则它会漏进所有下游消费点（列表/统计/批次/导出/待办）**。当一张表用 `type` 区分多种记录，且有个共享的 `listXxx(proj)` 被 N 处（主列表、聚合统计、批次归并、CSV 导出、待办、客户计数…）复用时：新增一个**不该出现在这些地方**的类型（如「会话记录」只该进对话记录抽屉、不进工单收件箱）后，若只在个别端点按 type 过滤、没在 `listXxx` 源头排除 → 它会**静默混进**每个未显式过滤的消费点（统计虚高、导出多行、批次误纳）。做法：新增「非工单/非主实体」类型时，先 `grep` 该 `listXxx` 的**全部调用方**，在 `listXxx` **源头**加 `arr.filter(e=>e.type!=='新类型')`（一处挡住所有下游），个别确实要看它的端点再单独放行。（来源：intake FS-04，`intake-conv` 若不在 `listIntake` 排除会漏进 inbox/批次/统计/导出/待办。）
- [ ] **多轮 AI 对话里「服务端剥掉了某些块再回前端」时，历史会丢关键状态（已归档/已建单/已处理）→ 下一轮 AI 据残缺历史误判（重复/合并/翻旧）；防上下文污染用「代码维护水位线切段」而非只靠提示词**。当一个 AI 对话流会**边聊边产出结构块**（`intake-record`/工具调用/归档指令），服务端解析建单/执行后把块 `replace` 掉再返回前端、前端存的就是剥块后的文本时：前端把整段历史发回下一轮，AI **看不到"某条已归档/已建单/已闭环"**（那信息随块被剥了）→ 把已处理的旧需求当"还在讨论"，与新需求**重复建单 / 合并揉成一条 / 反复翻旧**。别指望靠提示词「请记住 X 已建单」硬约束（AI 不可靠遵守）。做法（确定性）：**加一个「水位线」游标**（`filedUpTo` = 已处理覆盖到第几条消息），① 前端每次产生"已处理"副作用后把水位线上移到 `messages.length`、随请求上送；② 服务端用代码把历史切两段——`archived=slice(0,水位线)`（折叠成**一条**「只读背景·禁止再处理/合并」说明）+ `active=slice(水位线)`，**只对 active 判断有没有新的要处理**；③ 提示词与之呼应（「只对当前待处理段判建单、背景只读」）。边界：水位线 0 / 越界夹回 / 老前端不传 → active 为全量 = 行为同现状不回归；**落库/持久化仍用真实完整对话**（别落进折叠的合成背景说明）。水位线随草稿/快照 save-restore、新会话归 0、reopen 已处理会话设满、未处理设 0。测试：造「先处理 A（水位线上移）→ 提无关 B」，断言喂模型的 active **完全不含 A**（确定性隔离），且真模型冒烟里 B 轮回复不翻出 A、A/B 各建独立一条。（来源：intake FS-04 对话进件，`intake-record` 块建单后被剥、AI 把已建单旧需求跟新需求合并；`filedUpTo` 前后端水位线切段修复。）
- [ ] **让 AI「识别到 N 条就产出 N 个结构块」不可靠——它会用"打包/一起处理/已登记"这类话把多条揉成一个块或漏出；服务端 `matchAll` 再健壮也救不了「AI 只出一个块」**。当建单/建任务靠「AI 每条各产出一个结构块、服务端 `matchAll` 有几块建几条」时，即便服务端支持多块，AI 常在明确拆分出 N 条后只出 1 个块（漏建其余）。提示词要**下狠约束**：「一旦识别/确认/拆分出 N 条独立项，**必须**为每条各出一个块，N 条即 N 个块，**少出一个块=漏一条=错**；'打包/一起转开发/已登记'只是**话术**，不改变'一条=一块=一条记录'；部分齐则已齐的**现在就出块**、未齐的继续追问，别为了'一起提交'而都不出。」真模型冒烟必测：一轮说两条 → 断言真的建了两条（不是一条）。（来源：intake FS-04，AI 说"已拆分为两个需求…打包转开发"却只建 1 张单。）
- [ ] **AI 从对话里「建单/建任务/下工具调用」这类有副作用的动作，别让 AI 直接执行——改「AI 出结构化计划 → 用户在 UI 上确认/编辑 → 代码按清单确定性执行」（提示词压不死"该建几个/合并/追加"，确认清单一步治本）**。当"对话 → N 个副作用"（建 N 张单 / N 个任务 / N 次写操作）靠 AI 每次乖乖输出对的结构块时，即便加了强约束提示词 + 服务端 `matchAll`，仍压不住两类失效：① **多条被揉成一个**（AI 觉得相关就合并）；② **续聊时新内容被无脑追加进旧记录**（续聊路径 append 到上次那条）。**治本**（不再靠 AI 自觉）：把动作拆成三段——(a) **AI 只出「计划」**（一个结构块 `xxx-plan`，`items:[{action:'new'|'append', targetId?, ...}]`，**一条独立诉求=一个 item·绝不合并**，每 item 带 `summary` 供 UI 展示；续聊传入"已存在记录清单"让 AI 判 append/new，**默认 new**——宁可用户改成 append，别默认合并）；服务端**解析计划但不执行**，只把 `items`（补服务端兜底字段）回前端；(b) **前端渲染可编辑「确认清单」卡**（改标题/摘要、切 新建/追加到某条、删条、**拆成两条**、加条），**用户拍板前不产生任何副作用**（可继续对话让 AI 重出计划，新计划把旧未提交卡标灰=已取代，防对着过期计划点确认）；(c) **确认 → 调独立「commit」端点按清单确定性执行**（`new` 逐条建、`append` 校验目标存在+权限(按数据范围收敛,越权跳过不报错)+留痕，返回 `{created,appended}`）。**续聊也必须接确认清单**（别再走"无脑 append"的旧路径）。老的"AI 直接出建单块"路径**不保留**（统一走 plan，避免两套）；水位线/幂等/会话分组/落库真实对话等既有机制**复用不变**。**权限白名单**：新 commit 端点记得进所有相关白名单（访客链接原能经旧路径建的，新端点也要放行，否则访客建不了）。真模型冒烟必测四场景：一段两条→2 条独立、续聊无关新诉求→new、续聊补充→append 到旧条不重复、确认卡拆条→2 条。（来源：intake FS-04 现场对话建单，v1「水位线+N块强约束」仍压不死合并/续聊追加，v2 改「AI 出 intake-plan → 确认清单 → /api/intake-commit-plan 确定性建单」治本。）
- [ ] **重命名 / 换掉一个被「源码级静态测试」grep 的函数或改其调用（`kbRetrieve`→`kbRetrieveScored`、抽模板进新模块…）时，先 `grep` 出所有 `SRC.match(/…函数名…/)` / `SRC.slice(...)` 静态断言，同步改到位——行为不变但静态匹配会突然红**。当项目有"抽真身 eval"或"静态源码字符串匹配"型测试（`assert.match(SRC, /hits = await kbRetrieve\(.../)`）时，它们**绑死具体源码文本**：一旦把 `kbRetrieve(...)` 改成 `kbRetrieveScored(...).map(x=>x.e)`、或把提示词挪进别的模块，这类断言即失败（**不是功能坏，是文本对不上了**）。改动前 `grep -l "被改函数名\|被改文案片段" tools/*.test.mjs` 列出所有静态依赖点，逐个把正则/切片更新成**新的等价文本**（并在断言 message 里注明"结构变、行为不变"）。特别注意"计数型"断言（如"全仓仅 1 处调用 X 传 minScore=2"）：新增一个刻意复刻同口径的调用点（诊断/回放端点）会让计数从 1 变 2，要把预期数一起调（并说明第 2 处是刻意复刻）。（来源：intake PD-03，consult 从 `kbRetrieve`→`kbRetrieveScored`，fs-06 的 `SRC.match(/hits = await kbRetrieve.../)` 与"仅 1 处 minScore=2"计数断言双双需跟改；与已有"提示词外部化后 extractFn/SRC.slice 老测试变红"同类。）
- [ ] **算「剩余 N 天 / 到期提醒」用 date-only 归一，别拿带时分秒的 `Date` 相减除以 86400000 —— 会 off-by-one**。「维保/证件/会员到期」这类「今天到到期日还剩几天」的计算，若两边是**含时刻**的 `Date`（`new Date('2027-03-31')` 是 UTC 00:00、`new Date()` 是本地此刻），相减 `/86400000` 再 `floor/round` 会因**时刻差 + 时区偏移**飘一天（如剩 15 天算成 14 或 16，边界 `≤15` 判定就错）。正确：**两边都归一到「纯日期」再算整天差**——`const end=Date.UTC(y,mo-1,d)`（把 `yyyy-MM-dd` 拆成 y/mo/d 传 UTC）、`const today=Date.UTC(now.getFullYear(),now.getMonth(),now.getDate())`（今天用**本地**年月日再喂 Date.UTC，抹掉时刻），`daysLeft=Math.round((end-today)/86400000)`。两端都归一到同一「UTC 午夜」基准，差恒为整天、无时区漂移、无 off-by-one；负数=已过期。**前后端各算一份时口径要一致**（都 date-only），否则前端标黄/后端提醒不同步。测试必造边界四例：今天(0)、阈值(如+15 命中)、阈值+1(+16 不命中)、-1(已过期命中)，断言 `daysLeft` 精确值而非只测命中与否。（来源：intake FS-05 维保到期提醒，阈值 ≤15 天。）
- [ ] **RAG 答疑「据检索证据作答」的覆盖判据用「相关/能否帮上忙」，别用「是否精确覆盖那个点」——后者门槛太高会把大量相关问题一刀切拒答**。给 RAG 答疑提示词加「不臆造」护栏时，很容易走极端把判据写成「检索到的规格/源码**是否直接讲到了用户问的那个具体点**，没讲到就一句话说没覆盖」——结果**过严**：明明检索到**高度相关**的同功能/同模块内容（如问「床位号查不到患者」命中「患者列表与筛选」16.8 分 + 患者查询路由），却因「没逐字提到床位号」被拒答，用户抱怨「基本问题都答不出来」。正确的平衡点分三档：① **只要检索内容与问题相关**（同功能/模块/主题、能帮排查或理解）→ 就据真实内容尽量把问题讲清楚、给排查方向，**不要求 100% 精确覆盖**；② **红线只卡在「不臆造具体技术名」**——不编造规格/源码里没出现的**具体**表名/字段名/接口路径/配置 key/开关名（拿不准就说"具体的表/字段这块没写明，建议转工单/问开发"），但**一般性排查思路可以给**；③ **只有检索内容跟问题基本不相关**（明显别的模块/主题，如问「检验异常值箭头」却只召回「登录鉴权/患者外网自助」）→ 才一句话说没覆盖、不硬编步骤。核心：**「相关就答、只是别把猜的具体名当事实」≠「不精确覆盖就拒答」**。收紧「不臆造」时把红线卡在**具体技术标识符**上，而不是卡在「是否覆盖」这个太粗的闸门上。（附：无关经验召回的过滤该在**检索层**做二次相似度门槛，别靠提示词一刀切拒答来兜——提示词只管"相关时怎么答/不臆造"，别兼职当召回过滤器。）（来源：intake consult 答疑，上一版「精确覆盖才答」过严→再平衡为「相关就答、不臆造具体名」；CHG-consult答疑-覆盖判定再平衡-相关就答不臆造具体名。）
- [ ] **要 AI 输出「排查/操作步骤」时，编号 1/1/1 变 1/2/3 是「渲染器 + 提示词」两半的活，缺一半都白搭**：让模型给可照着做的排查步骤，常见两个坑要一起治——① **模型爱给每个主步骤都写 `1.`**（它把每步当独立有序列表的第一项），若前端 markdown 渲染器**丢弃源码序号、按出现顺序自己重排**，1/1/1 反而会被"修好"成 1/2/3（掩盖问题）；反过来若渲染器**忠实按源码 `<ol start=>` 渲**，模型全写 1. 就真渲成 1/1/1。**治本要两半都对**：(a) 渲染器忠实按源码起始序号渲有序列表（`start` 属性）；(b) **提示词明确要模型"主步骤连续递增 `1.` `2.` `3.`、绝不每步都写 1."**——只改一半都会 1/1/1 或被掩盖。② 顺带在提示词规定「**每个编号步骤=一个明确动作**（去哪个页面/点哪个按钮/看哪个值/这步看到什么算正常）、**细节用 `-` 子项列在该步下**、**别在两个编号步骤间插顶格大段文字**（会打断编号连续性）」——否则步骤含糊、或大段文字把有序列表截断成多个"从 1 重来"的小列表。这属**输出格式规范**，与"覆盖判定/不臆造具体名"等**行为规则正交**：加它别动那些红线（没覆盖仍一句话拒答、步骤里照样不臆造具体表/字段/接口名）。若提示词有「逐字参照」型回归测试（`renderPrompt(默认)===参照`），记得把新规则同步进参照的对应分支（normal/deep 都加），否则逐字断言变红。（来源：intake PD-02 consult 答疑，渲染器已改「有序列表带真实 start」后，再给 consultNormal/Deep 加「排查步骤连续编号 + 明确操作」格式规范，两半合起来才不再 1/1/1。）
- [ ] **Element Plus `el-select multiple`（含 `allow-create` 可自定义）的 v-model 必须始终是数组——后端把该字段以 null/单值下发时先在加载处归一为 `[]`，否则控制台告警且渲染异常**：当一个「多选/可自定义标签」字段（如联网来源、标签、限定类型）由后端 JSONB/可空列回来，空值是 `null`（或历史脏数据是单个字符串）时，直接绑到 `el-select multiple` 会触发 Vue 告警、tag 渲染错乱。**在加载详情/进入编辑态的同一个归一化循环里** `if (!Array.isArray(x)) x = []`（连同其它多选字段一起），别指望模板里 `(x||[])` 兜住 v-model 双向绑定。配套：① `allow-create` 建议加 `:reserve-keyword="false"` + `default-first-option`，输入体验才顺；② 分组预置用 `el-option-group`，同时 `allow-create` 仍可输入组外自定义值；③ 「清空该多选」提交给后端「null=不变」语义的接口时用**空数组 `[]`**（非 `null`）覆盖清除（`null` 会被后端当"不变"跳过、清不掉旧值）。（来源：zenlin ZL-104/ZL-201 tsubqa7 题目「联网来源」多选，web_sources JSONB 可空列回 null。）
- [ ] **LLM 给的「引用/来源 URL」几乎必有幻觉（编造打不开的链接）——只校验「有没有 URL / 结构完整」远远不够，要真探测「能不能打开」，但按三态处理别一刀切删**：让模型带可核验引用（citations 里 source/url/quote）时，别止于「字段齐不齐、是不是 http(s)」——大量 URL 是模型现编的、直接 404。加一层**轻量存活探测**再决定去留：① 优先 HTTP `HEAD`，服务端不支持（405/501/400 或抛异常）回退 `GET` 只取头/首字节（带 `Range: bytes=0-0`，别下整页）；② **短超时（per-URL ~3-4s）+ 小并发（≤4）+ 单批 URL 上限（如 ≤20）+ 整体封顶**——这类校验常在**后台异步**链路里，可容忍几秒但**绝不能挂死**主流程；③ **三态判定，不是二值**：`2xx/3xx→OK 保留`、`404/410/400→DEAD 丢弃`（明确不存在）、`403/405/429/5xx/连接超时/DNS 失败→UNVERIFIED 保留但打标记`（**很可能是站点反爬/鉴权/临时故障，删了就误杀真实来源**）；④ **非 http/https（本地文件路径等）跳过、不探测**；⑤ **探测器自身任何异常/超时 → log warn 保留原引用、不阻断主流程**（宁可放过也别把整条评分/生成搞挂）；⑥ 丢弃的死链记进一个 `droppedXxx` 字段供追溯。配套：给个全局开关（默认开）可整段跳过。别用 `followRedirects=ALWAYS` 顺带把 3xx 判成最终页——3xx 本身即视为可达即可。（来源：zenlin ZL-104 tsubqa9，AI 评分引用 URL 幻觉，`CitationReachabilityChecker` 用 JDK17 `java.net.http.HttpClient` HEAD→GET 回退。）
- [ ] **「基线行 + 覆盖行」分层配置：按维度合并、别整行覆盖——一行里某个维度为 null 绝不能把基线该维度压没（隐藏遮蔽 bug）**。凡「全局/国家基线一份 + 地区/租户/环境覆盖一份」的分层取值（同一实体 `(id, scope)` 多行、每行含**多个属性维度**）：读取常被写成「查到覆盖行就整行返回、忽略基线」。两个坑：① **业务上可能要求「基线是硬底线、覆盖只能增强不能削弱」**（如法定必含项、最低配额、安全默认开）——整行覆盖会让覆盖行把基线的强值降级，违反语义。正解是**逐维度合并**：每个维度按其业务单调性取值（布尔类「或」/并集、枚举类按强弱序取更强、数值类取 max/min），基线值作下限。② **更隐蔽**：覆盖行往往只为**某一个维度**而建（如只为打「地区集采标记」建行，另一个「是否基线必含」维度留空），整行覆盖会让这个**空维度**遮蔽基线的真实值 → 该维度被误判「未维护/否」。**absence 语义要显式定**：查不到覆盖也查不到基线时，某些维度（有权威完整目录背书的）应默认判定值（如「否」）、另一些（无完整数据的）保守留「待维护」——两类维度 absence 处理不同，别一刀切。**加系统未就绪安全网**：若整表连基线都从没导入（如目录未初始化），把「absence→默认判定」回退成「待维护」，避免全量误判。测试必造**遮蔽回归用例**：基线某维度=强值 + 覆盖行该维度=null（只填了别的维度）→ 断言合并结果**仍取基线强值**（不被 null 遮蔽），这条最容易漏、最能抓住整行覆盖的 bug。（来源：zenlin ZL-303 tdrugprov19，`zenlin_drug_province_attr(drug_id, province)` 一行同存 is_essential+procurement_status，旧「省行整行覆盖」让省集采行的空 is_essential 遮蔽国标基药；改「国标行+省行按维度合并、基药并集国标底线、集采取更强」。）
 - [ ] **「相对/同组比价」型评分（得分靠同组其它对象相对算出，非每对象独立）：先判「本题在这个组有没有有效比价集」（无 → 赋满分/中性值别硬算）→ 取同组【同可比维度】对象（不同途径/单位/类别排除，别混比）→ 带单位量归一到同一基准（质量→mg、体积→ml，跨基准判不可比）→ 求最低/基准 → 算不出的对象排除出基准、本对象算不出/缺数据 → 待人工（写「待维护」证据、不判 0 不猜）→ 得分 BigDecimal 封顶满分下限 0、证据写清「本对象值/组内基准/公式/各来源/排除数」**。带单位自由文本（规格/容量/剂量）解析要先拦截明确算不出的形态（含 `:` 浓度/多组分、`万单位`、`相当于`、`配方颗粒`、无数值）判待人工，别让正则抽出假数；「总量=单位量×数量」的数量解析不到给显式默认+记日志。试评分拿不到组上下文→给说明性 note 不出分不报错。单测造：本对象最低→满分/=2×基准→满分×0.5/不同维度排除/复方浓度型→待人工/缺数据→待人工/分流不匹配→满分；连真数据造「组+≥2 对象带真价真规格」跑一遍验算术+真列可算。交付统计规格解析比例（复方/浓度型落待人工是预期），并核对价格/单位录入口径与算法假设一致。（来源：zenlin ZL-104 AC-46 经济性组内相对比价，见 [[L-020]]。）
 - [ ] **同一主机两端口（或两独立域名）想「各自独立登录」：session cookie 必须按域取不同名——cookie 不区分端口（RFC 6265），同名会共用一个 session**。多子应用/多端部署在**同一 host 不同端口**（如 `:5180`=运营、`:5181`=实施）时，浏览器**cookie 不按端口隔离**（RFC 6265：`http://h:5180` 与 `http://h:5181` 是同一 cookie 作用域）——若两端登录 cookie **同名**（如都叫 `sess`），一端登录写的 cookie 另一端也读得到 → **一端登录另一端也登录、一端登出牵连另一端**，实现不了「各登各的」。同域名不同端口如此；**同一 IP 直连**更是如此。解法：**按域给 session cookie 起不同名字**——判出当前请求属于哪个「逻辑域/端」（按 `Host`/反代透传的域名，或部署配置的 origin 映射）→ cookie 名带域后缀（`sess_admin`/`sess_field`）；浏览器里两个 cookie 都存（同 host），但**每个端只读/写自己那个名** → 会话独立。**只改承载 token 的 cookie 名，session 存储（token→userId 表）不动**。四处必须同口径：① 登录 `Set-Cookie` 用带域名字；② 读当前用户（`currentUser`）按同一域算 cookie 名取 token；③④ 登出两处（API + 页面跳转）**只清「当前域」那个 cookie**（`Max-Age=0`），别误清别的域的（否则登出一端连累另一端）。**向后兼容**：判不出域（单域名/直连 IP/本机开发）→ 回退**原 cookie 名**，老部署行为零变化。⚠️ **上线后所有人会重登一次**（cookie 名变了，旧同名 cookie 在带域模式下读不到）——预期，交付说明注明。测试：`cookieName(origin)` 三分支（field/admin/other）+ 源码级断言 login/logout×2/currentUser 都走该 helper（无残留裸旧名）+ 连真服务冒烟（admin 域登录拿到的 cookie 打 field 域 `/me`→未登录，证两端独立）+ 单域名实例仍下发原裸名（证零变化）。别混淆另一套 cookie（如免登录链接身份 cookie）——只改 session cookie。（来源：intake FS-08 AC-15b，新机 `:5180` admin/`:5181` field 同 IP 两端口共用 `intake_sess`→改按域取名 `intake_sess_admin`/`intake_sess_field`、other 回退 `intake_sess`。）
 - [ ] **在「已在跑的 Vite dev server」里新增 npm 依赖 → 必须重启 dev server 才会预构建（optimizeDeps），HMR 不会自动拉起新依赖**。给一个 dev 已启动（`npm run dev`/`vite` 常驻、HMR 热更）的前端加运行时依赖（如 `xlsx`/`echarts`/`dayjs`）：`npm install` 只落 `node_modules`，Vite 的**依赖预构建（esbuild optimizeDeps）在 dev server 启动时扫描**、运行中**不会**因你 import 了新包而自动补构建。表现：`import * as X from 'xxx'` / 动态 `import('xxx')` 首次触发时报 `Failed to resolve import` / `504 Optimize Dep` / 白屏，但 `vue-tsc`/`tsc` 类型检查**照过**（类型解析走 node_modules 的 `.d.ts`，与 Vite 运行时预构建两条独立通道）——**类型绿 ≠ 运行时能跑**，容易误判「装好了」。防复发：① 新增依赖后**明确要求重启 dev server**（若 dev 由别人/编排器管、你自己不能重启，就在交付里**显式写「已加 X 依赖、需重启 dev 生效」**，别默认对方知道）；② 用**动态 `import('xxx')` + try/catch** 兜住加载失败（未重启/装失败时给可读提示、走降级路径，别整页崩）；③ 别拿 `vue-tsc --noEmit` 通过当「依赖可用」的证据——它证的是类型，不是 Vite 能解析。（来源：upwd-saas portal 合并上报页加 `xlsx` 解析 Excel，dev@5174 常驻、vue-tsc 绿但 xlsx 需重启预构建才生效。）

---

## 🔄 全局 CLAUDE.md 镜像（自动同步 · 通用规范）
<!-- AUTO-SYNC:global-claude-md START -->
> ⚙️ 本块由 hook 自动镜像自 `~/.claude/CLAUDE.md`（claude 写全局记忆时同步过来）。**勿手改本块**——要改去改全局 CLAUDE.md。

# 全局工作规范（所有项目共用）

> 本文件被每个项目的 Claude Code 会话自动加载，是**跨项目通用约定**。
> 项目级 `CLAUDE.md` 与此冲突时以项目级为准；此处只放与具体项目无关、可复用的规范。

## 前端 UI 通用规范

适用于所有前端项目的**列表页 / 后台管理界面**。新页面默认照此实现，已有页面逐步对齐。
（来源：病案平台 pams 的用户反馈，沉淀为通用规范。）

1. **菜单分级分组**：侧边/导航菜单按业务域**分组分级**（父分组 + 子菜单，可展开/折叠），不要一长串平铺罗列。

2. **菜单稳定排序**：菜单项按固定 `sort` 排序，顺序**稳定且符合业务优先级**；不要依赖插入顺序或随机顺序。

3. **表格内部滚动，禁止整页滚动**：列表页表格区域**自身内部滚动** + 表头 sticky 固定；页面外壳固定视口高度，**不出现 body 全局滚动条**。
   骨架：外壳 `height:100vh; box-sizing:border-box; overflow:hidden; display:flex; flex-direction:column`；工具条/分页 `flex:0 0 auto`；表格外层 `flex:1; min-height:0; overflow:auto`；`thead th { position:sticky; top:0 }`。

4. **搜索/筛选条件可收缩**：表头筛选区**超过一行**时做成可展开/收起——默认只显示第一行 + "展开/收起更多"，避免 2+ 行筛选挤占垂直空间、压缩表格可视内容。

5. **内容卡片紧凑**：主内容卡片 **padding 收紧**，减少大块留白与间隙，保持紧凑美观（避免默认大 padding 把表格挤到很小一块）。

6. **造数验证分页**：每个功能模块交付时**造一批足够触发多页的样本数据**，端到端验证分页（翻页、每页条数、总数统计）正确，而不是只有几条看不出分页问题。

7. **统一"返回门户"入口**：多子应用 + 门户的架构下，**每个子应用都要有统一的"返回门户"入口**（prod 跳同源门户 `/`、dev 跳门户开发端口）；不要某些子应用有、某些没有。

8. **列表搜索条件持久化**：列表页的搜索/筛选条件 + 分页（页码、每页数）要**记录下来**——从列表点进详情、再返回列表时，**原筛选条件与所在页要保留**，不能被重置成默认。实现可用路由 query 同步、`keep-alive` 缓存列表组件、或状态store；返回路径要能命中缓存。

9. **起止日期用单个范围框**：开始时间/结束时间这类成对条件，做成**一个范围控件**（一个单元格内"起 ~ 止"），**不要拆成两个独立筛选格**——否则栅格换行时会把一对拆到两行，很别扭。label 用合并名（如 申请起/申请止 → "申请日期"）。

10. **栅格筛选列数统一**：同一系统所有列表页的筛选栅格**列数一致**（如统一一行 4 个），列宽对齐；折叠时也保持满行（首行就是完整一行 N 个，不要收起后只剩 N-1 个 + 空格）。"展开/收起"只在筛选项确实超过一行时才出现；不足一行的页面不显示折叠按钮。

11. **选择类筛选即选即查**：**下拉/单选/多选/日期(范围)选择器/可搜索下拉**等"选择型"筛选项，**选中即触发查询**（`@change` → 重置到第 1 页 + 重新请求接口），**不用再点"查询/搜索"按钮**。只有**自由文本输入框**才靠回车或点搜索按钮触发（避免每敲一个字就请求）。所有列表页、所有子系统统一此交互。

12. **时间统一格式 `yyyy-MM-dd HH:mm`**：所有**展示**页面（列表列、详情、抽屉、卡片…）的日期时间一律格式化成 `yyyy-MM-dd HH:mm`（年-月-日 时:分，24 小时制），**不要直接显示后端原始值**（如 ISO 的 `2025-04-09T18:58`、带毫秒/时区、时间戳）。用一个**共享格式化函数**统一处理（兼容 ISO 串 / 时间戳 / Date），空值显示占位（如 `—`）。纯日期字段无时间部分时可只到 `yyyy-MM-dd`，但凡有时间的统一到分钟。

13. **表单按真实库字段长度做校验（前后端双层 + 好的字段提示）**：长度等约束**对齐真实数据库列定义**（如 `varchar(20)` → 最多 20）——**先查真实表结构（如 `docs/db-schema.sql`）逐字段核对，禁止臆造**，查不到对应列就停下问人。
    - **前端**：输入项加 `maxlength` + 提交前校验；并**展示好字段提示**——让用户**提前看到约束**（如字数计数 `12/20` / `≤20字` 的 helper）、**校验失败精确定位到出错字段**（内联红字提示，别只弹一个笼统 toast）。
    - **后端**：save/update DTO 字段补 `@Size(max=N)`/`@NotBlank`/格式校验（与前端同一套长度），做**纵深防御**——绕过前端直调接口也不会超长入库报错；校验信息要能被前端清晰展示。
    - 数字范围、必填、格式（手机号/证件号等）同理按库字段约束前后端都校验。

<!-- AUTO-SYNC:global-claude-md END -->

---

## 📚 教训明细
> dev agent 把新教训追加在这里。

### L-001 搜索条件少的表格页，筛选按钮另起一行浪费纵向空间、视觉割裂
- 范围/模块：通用 · 前端列表/表格页布局
- 根因：默认把筛选条件区和「查询/重置」按钮分两行排，字段少时显得空、割裂。
- 解法：搜索条件少（≤2~3 个）时，把按钮**直接紧跟在搜索框后面、同一行**，不另起一行。
- 防复发：列表页布局先数搜索条件数量——少就单行（按钮跟随）；多（或带高级筛选）才换多行/可折叠。
- 关联：UI 规范，无 spec。

### L-002 单个简单筛选项还放前置 label，与 placeholder 重复多余
- 范围/模块：通用 · 前端列表/表格页筛选区
- 根因：每个筛选项习惯性配一个 `<label>` + 输入框，但当全页只有 1 个简单搜索框（或 1 个下拉）时，label 文字和输入框 placeholder 内容基本重复（如 label「箱编码/箱名」+ placeholder「箱编码 / 箱名」），白占空间、看着啰嗦。
- 解法：**筛选项只有 1 个简单文本搜索框 / 单个下拉 → 去掉前置 label，仅留输入框（placeholder 已说明）**；只有当**同类筛选项有 2 个及以上**、需要逐个区分对齐时才放 label。例外：日期范围筛选始终保留合并 label（范围控件占位不自解释，见 UI 规范第 9 条）。
- 防复发：见自检清单对应项；新列表页先数筛选项数量决定要不要 label。
- 关联：[[L-001]]（同属"按筛选项数量决定布局"）；UI 规范第 9/10 条。

### L-003 派生脚本漏传分组字段，前端「按模块分组」静默退化、重启后才暴露
- 范围/模块：通用 · 派生/聚合数据脚本 ↔ 前端分组/筛选
- 现象：spec 文件头都写了 `module:`，前端也有「按模块分组」，但实际全归到「未标模块」；门户/数据中心等命名分组消失，且是服务重启重新生成 board.json 后才发现。
- 根因：`board.mjs` 把 spec frontmatter 派生成 `board.json` 时，`specs.push({...})` 只挑了 id/title/priority/status，**漏了 `module`**。前端按 `sp.module` 分组，拿到 undefined 就全落到「未标模块」——功能没报错、只是静默退化。
- 解法：派生 `board.json` 的 `specs.push` 补上 `module: (fm.module||'').trim()`；并把 5 个存量项目的 board.mjs 一并同步、重算看板。
- 防复发：见自检清单「派生数据时前端依赖字段必须一并透传」；改任何"源→派生数据"的脚本，先列清楚前端到底用了哪些字段，逐一核对别漏。
- 关联：UI 规范无；steward 控制台看板分组。

### L-004 飞书 wiki 文档读不到：光开 docx 权限不够，wiki 要单独开权限 + 共享知识库
- 范围/模块：通用 · 飞书开放平台文档接入（steward `/intake` / feishu-fetch）
- 现象：配好飞书自建应用、开了 `docx:document:readonly`，拉 `/wiki/` 链接报 `99991672 Access denied. scopes required: wiki:wiki / wiki:wiki:readonly / wiki:node:read`。
- 根因：知识库(wiki) 与云文档(docx) 是**两套权限**。`/wiki/<token>` 要先调 `wiki/v2/spaces/get_node` 解析节点（需 wiki 读权限），解析出的 docx 再读正文（才用 docx 权限）。只开 docx → 卡在 get_node。
- 解法：开放平台给应用加 `wiki:wiki:readonly`（+`docx:document:readonly`）→ **重新发布版本**（权限变更要重发才生效）→ 并把**该知识库共享给应用**（光有 scope、不共享具体知识库也读不到）。
- 防复发：见自检清单对应项；飞书接入先看"文档在哪类容器"再决定权限 + 共享。
- 关联：steward 飞书接入（feishu-fetch / `/intake`）；无 spec。

### L-005 飞书 raw_content 丢图片，需求藏在截图里 → 漏需求
- 范围/模块：通用 · 飞书文档接入（steward `/intake` / feishu-fetch）
- 现象：`/intake` 拉飞书需求文档后分诊、做出来验收，**第一项就没完成**——因为那条需求是用**截图**说明的，而 `docx/v1/.../raw_content` 只回纯文本，图片全丢（顶多剩个 `xxx.png` 占位）。
- 根因：飞书文档接口 raw_content 不含图片；需求文档又极爱用截图标注界面改动。只读文本 = 漏掉一半需求。
- 解法：feishu-fetch 额外用 `docx/v1/documents/{id}/blocks` 按序收集 image block(block_type 27) 的 token → `drive/v1/medias/{token}/download` 下载到 `docs/.intake/<docId>/` → 在输出末尾列出路径；`/intake` 让 claude **逐张 Read（视觉）** 并入分诊。下载需应用额外开 **`drive:drive:readonly`** 权限（光 docx 权限会 99991672）。
- 防复发：飞书接入要把"图片也取下来给模型看"当默认；缺 drive 权限时 fetcher 明确报"图片没拉到、先补权限别硬分诊"。
- 关联：[[L-004]]（飞书权限分容器）；feishu-fetch / `/intake`。

### L-006 顶栏用户名/头像整块绑了登出，点名字就直接退出登录
- 范围/模块：通用 · 前端顶栏用户菜单（多 SPA 子应用壳层 Layout）
- 现象：进子系统后点右上角「用户姓名」直接退出登录跳回登录页，预期应弹账户下拉菜单（修改密码/退出登录），登出只在点「退出登录」时发生。pams 三个子应用（BMS/用户中心/数据中心）全中。
- 根因：壳层 `Layout.vue` 把整个用户名区域 `.tb-u` 直接 `@click="logout"`（唯一可点项就是登出），点名字即执行。门户 Portal.vue 用 `el-dropdown` 包住才是对的——同种顶栏多处复制，一个写错往往全错。
- 解法：用下拉容器（`el-dropdown trigger="click" @command`）包住 `.tb-u`，破坏性动作放进菜单项、由 command 触发 + 二次确认；以项目里已正确的那个（门户）为范式照抄，多子应用全量对齐。子应用没有改密弹窗就只放「退出登录」。
- 防复发：见自检清单「打开菜单的可点区域别直接绑破坏性动作」；审顶栏/行操作时，凡"应该弹菜单"的可点块都核一遍它的 @click 是不是误绑了动作。
- 关联：pams CHG-2026-06-24-topbar-username-direct-logout。

### L-007 登录页要读的公开配置/品牌，复用了要鉴权的配置接口 → 登录前 401 拿不到
- 范围/模块：通用 · 登录页（登录前，无 token）读系统展示配置（Logo/单位名/主题色/品牌）；多 SPA 子应用各自登录页
- 现象：需求「4 个子系统登录页统一读医院配置展示品牌」。医院配置在 `sys_config.hospital_config`（JSON：logo/company/hospitalName），但既有读取接口 `/usercenter/config/list` **要鉴权**，而登录页在登录前、没 token；直接调会 401，登录页拿不到品牌、只能停在写死文案。
- 根因：把「登录后才用的、要鉴权的配置接口」当成「登录前也能用」的来源。鉴权拦截器只放行了 `/auth/login`，公开配置端点不存在。
- 解法：① 后端新开**免鉴权只读端点** `GET /auth/brand`，读 `hospital_config` JSON 但**只返 logo/company/hospitalName 三项**（不透传整份配置，防敏感项外泄）；缺失/空/JSON 非法 → 返回空串字段、不报错。② 白名单**精确放行** `/api/usercenter/auth/brand`（抽成 `WebMvcConfig.PUBLIC_PATHS` 常量、与 `/auth/login` 并列，并写自检测试断言含该路径）。③ Logo 图 `/file/SYS_LOGO/**` 由 nginx 同源根路径静态服务、**不经 `/api/usercenter/**` 拦截器**，登录前天然可加载，无需额外白名单。④ 前端 4 个登录页共用 `utils/brand.ts` `loadBrand()`：**原生 fetch** 直连，绕过带鉴权头/失败弹 toast 的 axios 拦截器；超时(AbortController)/网络失败/非 200 一律**静默回退**各页写死文案与默认图标，绝不阻塞登录。
- 防复发：见自检清单「登录前要读的公开配置/品牌必须单独开免鉴权端点 + 精确白名单」。凡「登录前/未登录页面要读后端数据」，先问：这条链路要不要 token？要 → 单开公开端点 + 精确放行 + 只返必需字段 + 失败静默回退。
- 关联：pams UC-01 AC-9 / UC-06（hospital_config 品牌公开消费）；[[L-037]]（sys_config 一行 JSON 存法，键名先查真库别臆造）。

### L-008 手写 SQL 持久层给已有表加列，只改 CREATE 语句不生效，存量库悄悄缺列
- 范围/模块：通用 · 手写 SQL 持久层（无 ORM 自动迁移）加字段
- 现象：给 `accounts` 表加 `enabled` 列，只在 `CREATE TABLE IF NOT EXISTS accounts (... enabled TINYINT DEFAULT 1)` 里加了列。全新库没问题，但**已存在**该表的存量库里 `enabled` 列根本没出现（`DESCRIBE` 无此列），upsert 到该列时会 `Unknown column 'enabled'`。
- 根因：`CREATE TABLE IF NOT EXISTS` 遇到已存在的表**整条跳过**，不会 diff 出新列去补。手写 SQL 层不像 Flyway/Prisma/JPA `ddl-auto` 会自动 ALTER。
- 解法：在 `init()` 里加幂等补列 helper——查 `information_schema.COLUMNS(TABLE_SCHEMA=DATABASE(),TABLE_NAME=?,COLUMN_NAME=?)`，无则 `ALTER TABLE ... ADD COLUMN`，可重复跑不报错（比 try/catch 吞 1060 更干净）；`loadAll` 映射对旧行 NULL 兜底（`enabled: r.enabled==null?1:(r.enabled?1:0)`）；upsert 列表 + ON DUPLICATE KEY UPDATE 都带上新列。本地已连真库验证：`DESCRIBE accounts` 出现 `enabled tinyint Default=1`，二次 init 幂等无报错，存量 2 行读回 enabled=1。
- 防复发：见自检清单「给已存在的表加列」；凡手写 SQL 层加字段，先想"存量库怎么补列"，别只改建表语句。
- 关联：intake AM-01（enabled 切片）；db.mjs `ensureColumn`。

### L-009 状态字段接受多种输入形态（中文/布尔/字符串），需归一后入库、编辑缺省保留原值
- 范围/模块：通用 · 状态/枚举字段的 save 入参归一
- 现象：`enabled`（启用/停用）前端可能传中文"启用"/"停用"、布尔、'0'/'1'；若原样入库 TINYINT 列会写进 0/垃圾值或误停用；且编辑抽屉不带该字段时若默认成 0，会把启用账号误停用。
- 根因：前端筛选下拉值是中文、提交可能是布尔/字符串，与存储码（TINYINT 0/1）不一致；save 对"未提供"与"提供 0"没区分。
- 解法：写 `normEnabled(v,def)` 统一映射（`0/false/'0'/'停用'→0`、`1/true/'1'/'启用'→1`、`null→def`）；**编辑**：`normEnabled(b.enabled, 当前值)` → 未带则保留原值；**新建**：`normEnabled(b.enabled, 1)` → 默认启用。前端状态标签也按码上色（启用 tag-success/停用 tag-gray），别按中文文案判。
- 防复发：见自检清单「入库前把中文/布尔/字符串状态值归一成存储码」；状态类字段 save 一律先过归一函数 + 编辑缺省保留原值。
- 关联：intake AM-01；[[L-008]]（同一切片）；server.mjs `normEnabled` / `pubUser`。

### L-010 状态机加新态：后端白名单/流转表/映射 + 前端硬编码镜像 多处联动，缺一静默错
- 范围/模块：通用 · 状态机/枚举扩展（后端权威 + 前端硬编码镜像）
- 现象：给工单状态机加两个新态（暂缓/已驳回）。若只在后端"合法态白名单"加值，会：漏改流转邻接表 → 新态无合法入口，请求全被"非法流转"拒；漏改"到旧粗粒度 status 的映射" → 新关闭类态归错聚合桶（不进「已关闭」计数）；漏改前端各页硬编码的镜像（状态下拉选项、状态色 class、操作按钮的"下一步可选态"）→ 下拉缺项/无颜色/操作台不出新态按钮。都不报错，静默错。
- 根因：一个枚举被后端多份结构（白名单 Set + 流转邻接表 + 映射函数）+ 前端多个页面各自硬编码的副本同时引用；前端没从后端拉、是抄了一份。加值只想到"主枚举"，忘了它的所有镜像。
- 解法：加值前 grep 枚举名 + 中文态字面量，列全引用点（本例：server.mjs `LIFECYCLE`/`TRANSITIONS`/`lifecycleToStatus` + detail.html `TRANS`/`TLABEL`/`lifeCls` + inbox.html `ST_CLS`/筛选下拉），逐处改；跑回归测试断言"旧链路仍通 + 新态可流转 + 非法目标/未知态仍被拒 + 映射进对的聚合桶"。
- 防复发：见自检清单「状态机/枚举加新值多处联动」。前端理想是从后端单一事实源拉枚举；硬编码副本时当"多份镜像"逐一同步。
- 关联：intake TK-01（工单状态机 + inbox.html/detail.html 镜像）；tools/tk-01.test.mjs。

### L-011 关键词检索：按「命中词种类数」排序会被大众词泛词大页淹没稀有词真答案，须 IDF 式稀有词加权
- 范围/模块：通用 · 关键词/全文检索排序（grep/倒排/RAG 召回喂给 AI 或列给用户前的重排）
- 现象：给 AI 答疑从源码/文档里 grep 出「最相关的几段」喂它。真答案文件只含稀有判别词（如「说明书」），但排不进 top-n；一堆蹭大众词（医嘱/列表/配置/药品，各命中几百文件）的"泛词大页"反而排前面，AI 拿不到真答案、只会答通用话术。
- 根因：两个叠加坑。① **多词切分后先插集合、不排序就 slice 截断词预算** → 大众 2-gram 先占满名额，判别性最强的长 n-gram / 英文标识（表名 `x_y`、驼峰方法名、接口路径段）被截掉，根本没参与检索。② **文件排序按"命中的不同词种类数"（terms.size）** → 一个泛词大页蹭到 6~8 个大众词，terms.size 高，压过只含 1~2 个稀有词的真答案页。两者都让稀有信号失效。
- 解法：① 截断词预算前**按特异性降序排**（英文标识 > 长 n-gram > 短 bigram：`spec = isIdent ? 100+len : len`），判别性词一定进预算。② 文件排序改**稀有词 IDF 式加权**：先统计每词文档频率 df（命中它的不同文件数），`weight(t) = lenBonus(t) / log2(2 + df[t])`（稀有 df 小→权重高、大众 df 大→趋 0；长词/英文标识再乘系数），文件得分=命中各词 weight 之和，按得分降序（次级按命中行数）。稀有词天然把真答案顶上来、压掉泛词大页。
- 防复发：见自检清单「关键词检索重排须按稀有度加权」。任何"多词命中→选 top-N 段"的检索（不止代码搜，文档/日志/RAG 召回同理），**别用命中词个数/种类数直接排**——那对大众词无免疫；用 df/IDF 给稀有词加权。造测试时要复刻"频率倾斜"：稀有词文件 df 小 + 一批泛词大页 df 大且 terms.size 高，断言修复后稀有词文件排 top、反证旧 terms.size 排序把它挤出（证明 bug 真实）。
- 关联：intake 深入思考 codeSearch（server.mjs）；tools/code-search-rank.logic.test.mjs；docs/changes/CHG-深入思考源码检索排序-稀有词加权.md。

### L-012 「主源 + 按共享键的旧数据兜底」列表：软删主源记录会被兜底路径按共享键复活
- 范围/模块：通用 · 任何「新主实体为主 + 缺主实体时按某共享键（sessionId/单号/外键）从关联子记录归组兜底」口径的列表 + 软删
- 现象：现场「对话记录」原先靠工单按 `sessionId` 归组。新增「会话记录」实体后改成「会话记录为主 + 旧数据（有工单无会话记录）按工单 sessionId 兜底」。给对话记录加软删，删掉某条**建过工单**的会话记录后——它没消失，反而换成 `fromConv:false` 的兜底形态又冒出来了（工单还在，兜底按其 sessionId 把这条会话重新聚合出来）。
- 根因：软删只针对「主源记录」（会话记录）打了 `deleted` 标记、列表也过滤了它；但**兜底路径**独立地从**关联子记录**（工单，本就该独立留存）按共享键重新聚合——它不知道「这个 session 的主源记录已被删」，于是复活。软删记录在数据收集阶段被 `if(e.deleted)continue` 滤掉，兜底更看不到它、无从规避。
- 解法：① **单独再扫一遍**缓存/库拿到「被软删的主源记录」的共享键，收进 `deletedKeys`（别指望已被 deleted 过滤的 raw 里还有）；② 兜底归组循环开头 `if(deletedKeys.has(key)) continue` 显式跳过这些键；③ 把「删主源 ≠ 删关联子记录」写进确认文案 + 交付说明——子记录（工单/明细）在各自清单里独立留存、独立删，删主源不级联。
- 防复发：见自检清单「主源+兜底列表软删要记 deletedKeys」。凡「A 为主 + 缺 A 时按共享键从 B 兜底」的列表，软删 A 必须让兜底认得「这个键的 A 被删了、别拿 B 复活」。测试造：A 建了关联 B → 软删 A → 断言该条从本列表**彻底消失** + B **仍在** B 自己的清单（连 prod 冒烟同款：删会话记录 → 对话记录空、工单仍在左侧提交清单）。
- 关联：intake FS-04 `/api/field/conversations`（`intake-conv` 会话记录 + `deletedConvKeys` 兜底跳过）；tools/fs-04-conversations.logic.test.mjs A4b；docs/changes/CHG-field-conversation-全量持久化与软删.md。

### L-013 多轮 AI 对话「服务端剥掉结构块再回前端」→ 历史丢已处理状态 → 下一轮重复/合并/翻旧；用「水位线切段」确定性隔离
- 范围/模块：通用 · 任何「边聊边产出结构块（建单/工具调用/归档指令），服务端解析执行后剥块再返回前端」的多轮 AI 对话流
- 现象：现场「提需求/报BUG」对话，同一次聊天里前一条需求已建单后，再提**互不相干**的新需求，AI 却把新需求跟已建单的旧需求**合并/重复处理**（回复变成"给旧需求加按钮 + 同时做新字段"两条揉一起）。
- 根因：AI 回复里的 `intake-record` 归档块，服务端建单后 `reply.replace(blockRe,'')` **剥掉**再回前端 → 前端 `chat.messages` 存的是剥块后的回复 → 下一轮把整段历史发回 AI 时，AI **看不到"某需求已归档建单、已闭环"**（那状态随块被剥了），于是把已建单旧需求当"还在讨论"，与新需求重复/合并。提示词「请记住 X 已建单」这类软约束 AI 不可靠遵守。
- 解法：**代码维护「已建单水位线」`filedUpTo`（确定性防污染，不靠提示词自觉）**。① 前端：`chat.filedUpTo`=已建单覆盖到第几条 messages，每次返回 `savedIds` 非空（本轮建了单）就上移到 `messages.length`（含本轮 user + 归档回复），发 `intake-chat` 带上；随草稿/快照 save-restore（夹 `[0,len]`）、`newConversation` 归 0、reopen 已建单会话设 `messages.length`、未建单设 0。② 后端 `/api/intake-chat`：收 `filedUpTo`（夹 `[0, allMsgs.length]`），切 `archived=slice(0,filedUpTo)`（折叠成**一条**「已建单归档·只读背景·禁止再建/合并」user 说明）+ `active=slice(filedUpTo).slice(-24)`，喂模型 `msgs = archived?[背景说明,...active]:active`——**只对 active 判是否建新单**。已建单旧需求落入只读背景、AI 无从翻出或合并。③ 提示词呼应（`hasArchivedBg` → 追加「只对当前待处理段判建单、背景只读禁再建/合并」）。④ **落库仍用真实完整对话 `allMsgs`**（工单 chat + 会话记录 convChat），别用折叠的 `msgs`（否则丢历史 + 落入合成背景说明）。边界：`filedUpTo=0`/越界/老前端不传 → active 全量 = 行为同现状不回归。
- 防复发：见自检清单「多轮 AI 对话服务端剥块→水位线切段」。凡「边聊边产结构块、剥块后回前端」的对话流，别让下一轮 AI 据残缺历史误判；加水位线游标、代码切「已处理只读背景 vs 当前待处理」，只对后段判处理。测试造「先处理 A（水位线上移）→ 提无关 B」断言 active 完全不含 A；真模型冒烟断言 B 轮不翻 A、A/B 各独立一条。
- 关联：intake FS-04 `/api/intake-chat`（`filedUpTo` 前后端水位线）+ `intakeChatSystem(...,hasArchivedBg)`；public/field.html chat.filedUpTo；tools/fs-04-intake-chat-sequential.logic.test.mjs；docs/changes/CHG-intake-chat-顺序流不重复不合并-N条N单.md。

### L-014 让 AI「识别 N 条就出 N 个结构块」不可靠：它会用"打包/一起处理"揉成一个块或漏出，服务端 matchAll 救不了「只出一个块」
- 范围/模块：通用 · 靠「AI 每条各产出一个结构块 + 服务端 matchAll 有几块建几条」的批量建单/建任务
- 现象：AI 明确拆分出多条需求并确认（「已拆分为两个独立需求登记：需求1…需求2…」），拍板后说「两条需求已闭环，打包转开发排期」——但**只建了需求1一张单，需求2没建**。识别了 N 条、只 materialize 了 1 条。
- 根因：服务端 `matchAll(blockRe)` 已"有几个块建几张单"，问题在 **AI 只出了一个块**——它用"打包/一起转开发/已登记"这类话把多条揉成一个块（或漏出）。AI 不可靠遵守「一条一块」的软性提示。
- 解法：提示词**下狠约束**——「一旦识别/确认/拆分出 N 条独立需求/BUG（哪怕说'拆成两条''打包转开发''已登记'），**必须**为每条各输出一个结构块，N 条即 N 个块，**少出一个块=漏建单=错**；'打包转开发排期'只是**话术**、不改变'一条=一块=一张单'；部分齐则已齐的**现在就出块建单**、未齐的继续追问，别为了'一起提交'都不出块。」（配合 L-013 的水位线一起做——水位线切段管跨轮不污染，本条管同轮不漏块。）
- 防复发：见自检清单「AI 多条→多块要下狠约束 + 真模型冒烟测建了几条」。任何"AI 每条各出一块 → 服务端各建一条"的流程，别信 AI 会自觉一条一块；提示词强约束 + **真模型冒烟造"一轮两条"断言真建两条**（不是一条），静态断言只能验提示词含约束、验不了 AI 行为。
- 关联：intake FS-04 `intakeChatSystem` 「N 条=N 个块」段；tools/fs-04-intake-chat-sequential.logic.test.mjs A1；docs/changes/CHG-intake-chat-顺序流不重复不合并-N条N单.md。

### L-015 让 AI 出「排查/操作步骤」时 1/1/1：是「渲染器按源码序号 + 提示词要连续编号」两半的活
- 范围/模块：通用 · 任何「AI 生成 markdown 答复 + 前端渲染」里要给可照做的排查/操作步骤的场景（RAG 答疑、诊断助手、工单处理建议…）
- 现象：答疑给排查类问题，答复内容对，但**主步骤全渲染成 1/1/1/1**（不是 1/2/3/4）；且步骤偏含糊、不够"照着能做"。
- 根因：两半各有坑。① 模型倾向把**每个主步骤都写 `1.`**（每步当成独立有序列表的首项）。② 前端 markdown 渲染器若**丢弃源码里的起始序号、按出现顺序自动重排**，会把 1/1/1 "掩盖"成 1/2/3（问题被藏住、换个渲染器又冒出来）；若渲染器**忠实按源码 `<ol start=>` 渲**，则模型全写 1. 就真渲成 1/1/1。任何一半没对，最终都可能 1/1/1（或被误盖）。③ 步骤含糊 / 编号步骤之间夹**顶格大段文字**，会把一个有序列表**截断成多个"从 1 重来"的小列表**。
- 解法：**两半一起治**——(a) 渲染器忠实按源码起始序号渲有序列表（输出 `<ol start=n>`，别自动重排）；(b) 提示词加一条**输出格式规范**：「主步骤用**连续递增**编号 `1.` `2.` `3.` `4.`、每步 +1、**绝不每步都写 1.**；每个编号步骤=**一个明确动作**（去哪个页面/点哪个按钮/看哪个值/这步看到什么算正常/不正常）；细节用 `-` **子项列在该步下**；**别在两个编号步骤之间插顶格大段文字**（会打断编号连续性）；先 1~2 句给结论再给步骤」。这条与「覆盖判定 / 不臆造具体表字段接口名」等**行为规则正交**，加它别松那些红线（没覆盖仍一句话拒答、步骤里照样不臆造具体名）。
- 防复发：见自检清单「要 AI 输出排查/操作步骤时，1/1/1→1/2/3 是渲染器+提示词两半的活」。① 改动前想清楚渲染器到底按不按源码序号渲，决定这个坑靠哪半根治（别只改一半）。② 提示词若有「逐字参照」回归测试（`renderPrompt(默认)===参照`），新规则要**同步进参照的每个分支**（如 normal/deep 都加），否则逐字断言变红。③ 真模型冒烟：问排查类问题，断言主步骤是**连续编号 + 每步明确动作 + 细节在子项**、不再 1/1/1。
- 关联：intake PD-02 `consultNormal`/`consultDeep` 加「排查/操作类答复的格式」；渲染器修复见提交 `9bcdf08 fix(ui): markdown 有序列表带真实起始序号`；tools/pd-02-prompts.logic.test.mjs 的 `refConsultSystem` 逐字参照；docs/changes/CHG-答疑排查步骤格式规范-连续编号明确操作.md。

### L-016 列表页「表格卡片够不到壳底、底部留白，且弹窗越多留白越大、同类页不一致」：append-to-body 弹窗的空壳吃掉 flex 壳的 gap
- 范围/模块：通用 · 任何「页面根壳 `display:flex; flex-direction:column; gap:Npx` + 页面里放了若干 element `el-dialog`（默认或显式 `append-to-body`）」的列表/表单页
- 现象：同一套「壳填满视口 + footer 贴底」的列表页，有的页正常、有的页表格卡片下方有一段留白；且留白大小与该页 `<Dialog>` 数量正相关（1 个弹窗→10px、2 个→20px、3 个→30px）。壳本身是填满视口的（迷惑点：只看壳会以为没问题）。
- 根因：`el-dialog` `append-to-body` 后本体被搬到 `<body>`，但组件在**原位仍留一枚空 wrapper div**（本仓是 `components/Dialog/index.vue` 的 `.base-dialog`）。页面根壳是 flex 列 + `gap:10`，这枚空壳（高度 0）却是一枚**真实 flex 子项**——排在表格卡片后面，壳的 `gap` 会在「表格卡片 ↔ 每一枚空壳」「空壳 ↔ 空壳」之间各插一段 10px。于是表格卡片（`flex:1`）的底被这些 gap 顶得够不到壳内底，差值 = 弹窗数 × gap = 底部留白。只复刻「壳+表」的 fixture 没有这些空壳，量不出 gap → 假绿。
- 解法：给这枚原位空壳加 `display:contents`（一条全局规则，作用于所有用该 Dialog 的页），让它**退出 flex 流**（不占 gap、不占位）。弹窗本体在 body 下、`.el-dialog__wrapper` 是 `position:fixed`，一律不受影响；即便哪天某处不 `append-to-body`，其 wrapper 同样 fixed，`display:contents` 也不改其定位。别去动壳的 `gap`（会波及所有共享该壳类的页、且要把 gap 改成 margin，blast radius 大）。
- 防复发：见自检清单「量壳/卡片是否填满时 fixture 要复刻完整高度链 + 真实组件残留节点」。凡「flex + gap 的壳」里放 append-to-body 弹窗，先想「原位留没留空 wrapper、会不会当 flex 子项吃 gap」。看守测试必须能抓这个坑：用真实 SFC 起 headless（`#app` 要真实存在、`.app-main` 要用组件真 scoped），或静态 fixture 把「空 wrapper × 弹窗数 + 壳的 gap」一起复刻；并做「删掉 fix→变红」的敏感性自检，别留假绿。
- 关联：petl-admin `components/Dialog/index.vue` 的 `.base-dialog{display:contents}`；`views/docking/source.vue`(1弹窗)/`views/hospital/campusArea.vue`(2)/`views/docking/list.vue`(3) 三页；看守 `petl-admin/tests/listPageFillNoBottomGap.test.mjs`（LF-1 源码契约 + LF-2 真机 CDP 三页×3/7/10 行）。同时把 `views/docking/list.vue` 的 `.table-stage` 补 `display:flex;flex-direction:column`，与共享基线 `.lp-table-stage` 对齐（消 el-table ~4px 竖向溢出）。

### L-017 分层配置「基线 + 覆盖」整行覆盖：覆盖行的空维度遮蔽基线强值 → 该维度被误判「未维护/否」
- 范围/模块：通用 · 任何「全局/国家基线一份 + 地区/租户/环境覆盖一份」的分层取值（同一实体 `(id, scope)` 多行、每行含多个属性维度）
- 现象：某维度业务上应恒有权威结论（如「是否法定必含/基线开关」），但只要给该实体的某个 scope 建了一条**只为另一个维度**的覆盖行（那维度有值、本维度留空），读取该 scope 时本维度就变成「未维护/否」——基线明明有强值却读不到。数据只有单一 scope（全用基线）时不触发，一旦启用分省/分租户覆盖立刻中招。
- 根因：读取写成「查到覆盖行就整行返回、忽略基线行」。但覆盖行往往**只为某一维度**而建，其它维度留 null；整行覆盖让这些 null 遮蔽基线真实值。且业务语义常是「基线=硬底线、覆盖只能增强不能削弱」，整行覆盖还会把基线强值降级，双重违反语义。
- 解法：改**逐维度合并**，不整行覆盖——取基线行 + 覆盖行两行，每维度按业务单调性取值：布尔/集合类「或/并集」、枚举类按强弱序取更强、数值类 max/min，基线值作下限（硬底线不被覆盖行的弱值/空值压低）。`scope=基线` 入参时基线行=覆盖行、只查一次且无「覆盖贡献」概念（否则会把基线结论误标成 override 来源）。**absence 分维度定**：查不到覆盖也查不到基线时，有权威完整目录背书的维度默认判定值（如「否」）、无完整数据的维度保守留「待维护」——两类不同别一刀切。**加系统未就绪安全网**：整表连基线都没导入（初始化前）→ 把「absence→默认判定」回退成「待维护」，避免全量误判（轻量 `count(is_not_null 维度) LIMIT 1` 探测）。
- 防复发：见自检清单「基线行 + 覆盖行分层配置：按维度合并、别整行覆盖」。测试必造**遮蔽回归用例**：基线某维度=强值 + 覆盖行该维度=null（只填别的维度）→ 断言合并结果**仍取基线强值**，这条最容易漏、最能抓整行覆盖 bug；再造 absence 两类维度（默认判定 vs 待维护）+ 未就绪安全网各一条。连真库跑（真实多维度列 round-trip），别只 mock。
- 关联：zenlin ZL-303 tdrugprov19，`DrugProvinceAttrService.resolve` 从「省行整行覆盖 + 全国回落」改「国标行 + 省行按维度合并（基药并集国标底线、集采取更强）」；`zenlin_drug_province_attr(drug_id, province)` 一行同存 is_essential + procurement_status。

### L-018 给多步编辑器加「未保存修改离开拦截」：deep watch 异步 flush 把同步清的 dirty 又置回 + 暂存原地 normalize 污染在编辑数据
- 范围/模块：通用 · 任何 Vue3 长表单/多层可增删配置编辑器（如 维度→子项→选项）加「防误操作（浏览器返回/关闭/刷新/侧栏跳转）丢内容」
- 现象：① 加了 `dirty` 跟踪后，**刚载入模板/刚点暂存回填就显示「未保存更改」**、离开时误弹拦截；② 若照搬「保存后跳转」的 save 逻辑做「暂存（存后留本页）」，暂存一次后**用户正在编辑的数据被悄悄改坏**（某些字段被清空、`sources` 消失）——因为 normalize 原地改了实时数组。
- 根因：① Vue `watch` 默认 `flush:'post'`（异步、在 DOM 更新后触发）。程序性「替换整个编辑数组引用」（载入/回滚/暂存回填）后**同步**置 `dirty=false`，会被随后 flush 的 deep watch（检测到引用变化）**重新置脏**——同步清脏赢不过异步 flush。② 原「保存草稿/发布」函数随即 `router.push` 离开，所以它对 `editDimensions` 原地 `normalizeSubitemConfig`（`delete sub.sources`、清空 AI 专属字段）无害（页面马上销毁）；但「暂存」要**留在本页继续编辑**，原地 normalize 就把用户正看着的实时数据污染了。
- 解法：① dirty 清脏用 `suppressDirty` 门 + `nextTick`：`markDirty(){ if(suppressDirty||!isEditing) return; dirty=true }`；`resetDirtyClean(){ suppressDirty=true; nextTick(()=>{ dirty=false; suppressDirty=false }) }`，在 进编辑态/新建初始态/载入/取消回滚/暂存成功回填 处调它兜住那一轮 flush。用户**真实交互**改动照常触发 deep watch 置脏。② 暂存落库前 `JSON.parse(JSON.stringify(editDimensions))` **深拷贝**再 normalize，别碰实时数组；成功后更新 `originalDimensions` 快照 + `resetDirtyClean`。③ 三件套：`onBeforeRouteLeave`（`vue-router` 具名导入）dirty 时 confirm、取消返回 false 拦住；`beforeunload` `preventDefault()`+`returnValue=''`（自定义文案被现代浏览器忽略）、`onBeforeUnmount` 移除监听。④ 保存/发布/暂存成功、取消放弃 都要 `dirty=false`（保存/发布会跳转，清脏防守卫再弹）。⑤「暂存 vs 存草稿并返回」区别做清楚：都存 draft，暂存**不跳转留本页**、存草稿**push 回列表**；新模板首次暂存 `createTemplate` 拿 id 后 `router.replace('/x/{id}?mode=edit')`（同名路由仅参数变、组件不重建、编辑态与数据不丢）、之后走 update。校验放宽：暂存只做最低校验（名称非空）、分值未过发布强校验也存只 warning，发布才走完整强校验。
- 防复发：见自检清单「给『多步编辑器』加未保存拦截」。目视核对必跑：编辑几处→点浏览器返回/返回列表→弹拦截；点暂存→toast 已暂存、留本页、再返回不再拦；刷新→beforeunload 原生确认；载入后/暂存后**不能**立刻显示未保存。暂存后继续改，确认字段没被 normalize 清掉（深拷贝生效）。
- 关联：zenlin ZL-201 ttplimp19，`zenlin-admin/src/views/admin/TemplateDetail.vue` `stashTemplate`/`resetDirtyClean`/`onBeforeRouteLeave`/`handleBeforeUnload`；纯前端、复用现有 draft 保存端点、无后端无迁移。

### L-019 同一主机两端口/两域名想各自独立登录：session cookie 同名会共用一个 session（cookie 不区分端口，RFC 6265）
- 范围/模块：通用 · 任何「多端/多子应用部署在同一 host 不同端口（或两独立域名共用一个后端进程）、要求各端独立登录会话」的场景
- 现象：把运营端/实施端分别部署到同一 IP 两端口（`:5180`/`:5181`）后，在一端登录，**另一端也变成已登录**；一端点登出，**另一端也被登出**——两端会话串在一起，实现不了「各登各的」。
- 根因：登录用的 session cookie **固定同一个名**（如 `intake_sess`），而 **cookie 不区分端口**（RFC 6265：`http://h:5180` 与 `http://h:5181` 属**同一 cookie 作用域**，Domain 按 host 不含 port）。同 host 下两端读写的是**同一个 cookie** → 同一个 session token → 共用登录态。同一 IP 直连、同域名不同端口都如此（换成两个**不同域名**才天然隔离，但同 IP 双端口部署做不到）。
- 解法：**session cookie 按「逻辑域/端」取不同名字**。① 加 helper `cookieName(origin)`：判出当前请求属 field/admin（按 `Host`、反代透传的域名、或部署配置 origin 映射）→ 返 `sess_field`/`sess_admin`，判不出（单域名/直连 IP/本机）→ 回退原名 `sess`（**向后兼容零变化**）。② 四处统一走它：登录 `Set-Cookie` 用带域名字；`currentUser`/读当前用户按 `cookieName(originOf(req))` 取 token；登出**两处**（`/api/logout` + 页面 `/logout` 跳转）**只清当前域那个** cookie（`Max-Age=0`），别硬编码清原裸名（会误清别的域）。③ **只改承载 token 的 cookie 名——session 存储（token→userId 表）、鉴权链路语义全不动**。浏览器里同 host 存两个 cookie（`sess_admin`+`sess_field`）互不干扰，每端只认自己那个 → 独立。
- 防复发：见自检清单「同一主机两端口/两域名各自独立登录：session cookie 按域取名」。测试：`cookieName` 三分支（field/admin/other）+ **源码级断言** login/logout×2/currentUser 都走该 helper（grep 无残留裸旧名，除 other 分支默认值/注释）+ **连真服务冒烟**（admin 域登录拿到的 cookie 去打 field 域 `/api/me`→`me:null`，证两端独立；单域名实例登录仍下发原裸名，证零变化）。别误动另一套 cookie（如免登录链接身份 cookie）。⚠️上线后所有人会重登一次（cookie 名变、旧同名 cookie 在带域模式读不到），预期须在交付说明注明。
- 关联：intake FS-08 AC-15b + §4④，`server.mjs` `sessCookieName(origin)`（field→`intake_sess_field`/admin→`intake_sess_admin`/other→`intake_sess`）+ 改 `/api/login`·`currentUser`·`/api/logout`·`/logout`；测 `tools/fs-08-session-cookie.logic.test.mjs`（三分支+源码断言）+ `tools/fs-08.test.mjs` D 组（连真库·真 server）。

### L-020 「相对/同组比价」型自动评分：得分依赖同组同类的其它对象（不是每对象独立），比价前必须按可比维度过滤、算不出的对象排除出基准、缺数据待人工不判 0
- 范围/模块：通用 · 任何「一个对象的得分/排名/占比由它所在【组】里其它对象相对算出」的评分（相对比价、组内排名、相对基准折算、市场份额、性价比排序……），及需从**带单位的自由文本**（规格/容量/剂量/尺寸）解析数值再比的场景
- 现象：把「经济性打分」当成和「基药/集采是否命中」一样的**每对象独立直读题**去做，会踩三类坑：① 只读本对象自己的字段就想出分——但相对比价**必须先取同组其它候选**求最低/基准，缺了组上下文根本算不出；② 不同「给药途径/口味/单位」的对象混在一起比（口服片 vs 静脉针、mg vs ml），比出来无意义；③ 规格文本形态千奇百怪（`10mg×30片`可算、`250ml:21.1g`/`0.25g(25万单位)`/`每1g配方颗粒相当于饮片5g`复方浓度型算不出），一律硬塞数值 → 给出**看似精确其实错**的分。
- 根因：相对评分与「属性直读/AI 单题评分」是**两种不同范式**——直读是「读本对象一个值→映射档位」，相对是「本对象值 ÷ 组内基准 × 满分」，依赖**同组同类对象集合**。把它塞进直读那套短路里，容易漏掉「① 取组集合 ② 按可比维度过滤 ③ 求基准 ④ 算不出的怎么办」四步中的后三步。带单位文本解析尤其危险：正则一把梭会把浓度型/复方/万单位规格也抽出个数来，静默产出错误单位金额。
- 解法：① **先判分流是否成立**——按分组类型/场景判「本题在这个组到底有没有有效比价集」，无有效比价集（如按通用名分的组里问"可替代比价"）→ **直接赋满分/中性值 + 证据注明规则**，别硬算。② **取同组集合 + 按可比维度过滤**：只在与本对象**同一可比维度**（给药途径/剂型归类/规格单位基准）的对象间比；不同维度**排除**出比较集（别混比）。③ **归一到同一基准再比**：带单位的量先折算到统一基准（质量→mg、体积→ml 各一类，跨基准判「不可比」而非强比）；「总量 = 单位量 × 数量」这类要把数量也解析出来（解析不到给显式默认+记日志）。④ **算不出的对象排除出基准**（不参与 min/max），**本对象算不出 → 该项待人工**（写「待维护」证据、**不判 0、不猜**）；复方/浓度型/多组分/无数值 等明确无法解析的形态**提前识别拦截**（含 `:` 分隔、`万单位`、`相当于`、`配方颗粒` 等关键词直接判「待人工」），别让正则抽出个假数。⑤ 得分用 BigDecimal、**封顶满分、下限 0**，证据写清「本对象值 X、组内基准 Y、得分=Y/X×满分」+ 各来源 + 排除计数，便于人核。⑥ 试评分/预览若**拿不到组上下文**（单对象试算）→ 给说明性 note「需在组内实际评分时算」，不报错、不出分。
- 防复发：见自检清单「相对/同组比价评分：先判比价集是否成立→取同组同类→归一基准→算不出排除/待人工不判 0」。单测必造：本对象是最低→满分、本对象=2×基准→满分×0.5、不同可比维度对象被排除、复方/浓度型规格→待人工、缺价/缺规格→待人工、分流不匹配→赋满分。**连真数据源冒烟**造「一个组+≥2 对象（带真实价格+规格）」跑一遍 min/本对象×满分，验证算术 + 真列可读可算（mock 抓不到「代码取的列 vs 真库列」错配）。⚠️交付里**统计规格/单位文本能解析的比例**给人参考——复方/配方颗粒/浓度型落「待人工」是预期非 bug。价格/单位的**录入口径**（整包价 vs 单支价）要与算法假设一致，历史数据口径不一会让单位量系统性偏，须在交付里点明请业务核对。
- 关联：zenlin ZL-104 AC-46 econ-relative-price，`ScoringService.scoreByEconRelativePrice`（组内同给药途径求最低单位金额=整包价÷规格总剂量、min/本药×满分封顶下限0）+ `parseSpecTotalDose`（g/mg/μg折算、复方/浓度/万单位→待人工）+ group_type 分流（generic 算同通用名/indication 算可替代，不匹配赋满分）+ dry 无组上下文给 note；测 `ScoringEconRelativePriceTest`(15例) + `ScoringEconRelativePriceRealDbSmokeTest`(真 PG 造组+2候选药验 0.1/0.2×3=1.5)。

### L-021 给「正在跑的 Vite dev server」新增 npm 依赖后不重启：类型检查(vue-tsc)照过，但运行时 import 解析失败/白屏（optimizeDeps 只在启动时扫描）
- 范围/模块：通用 · 任何 Vite 前端工程（Vue/React/…）在 dev server 常驻（HMR 热更）状态下**新加运行时依赖**（xlsx/echarts/dayjs/pdf-lib 等）
- 现象：dev server 已在跑（`vite`/`npm run dev`，HMR 正常），`npm install xxx` 成功、`vue-tsc --noEmit` EXIT=0，看着一切就绪；但页面里 `import * as XXX from 'xxx'` 或动态 `import('xxx')` 首次执行时报 `Failed to resolve import "xxx"` / `504 (Outdated Optimize Dep)` / 白屏——只有重启 dev server 后才好。容易误判「明明装了、类型也过了，怎么跑不起来」。
- 根因：Vite 的**依赖预构建（optimizeDeps，底层 esbuild）在 dev server 启动那一刻扫描**入口用到的裸模块并打成 `.vite/deps` 缓存；server 运行中**不会**因为你新 import 了一个此前没扫到的包而自动补构建（HMR 只热更你改的源码，不重跑 optimizeDeps）。而 `vue-tsc`/`tsc` 的类型解析走的是 `node_modules/**/*.d.ts`（编译期通道），**跟 Vite 运行时预构建是两条完全独立的路**——所以「类型绿」只证明 `.d.ts` 找得到，**不证明** Vite 运行时能解析该模块。两者被混为一谈就会误判「装好了」。
- 解法：① 新增依赖后**重启 dev server**（触发重新预构建）——这是让新依赖运行时可用的必要步骤；② 若 dev server **不由你掌管**（编排器/他人常驻、你无权重启），在交付说明里**显式写明「已新增 X 依赖、需重启 dev server 才生效」**，别默认对方知道要重启；③ 对新依赖用**动态 `import('xxx')` + try/catch** 包裹，加载失败（未重启/装失败）时给用户可读提示并走**降级路径**（如「解析依赖不可用，请改用 JSON」），别让整页崩、别阻塞其它不依赖它的功能；④ **别拿 `vue-tsc --noEmit` 通过当「依赖运行时可用」的证据**——它只保证类型，运行时能否解析要么真起 dev 验、要么留重启说明。
- 防复发：见自检清单「在已在跑的 Vite dev server 里新增 npm 依赖 → 必须重启 dev server 才会预构建」。凡「dev 常驻 + 加新前端依赖」，先问：这个 server 我能不能重启？能→装完重启再验；不能→交付显式注明需重启 + 新依赖动态 import 兜降级。
- 关联：upwd-saas portal 合并「上传+上报」页新增 `xlsx`(SheetJS) 解析 Excel，`portal/src/views/Submit.vue` 用 `await import('xlsx')` + try/catch（失败置 `excelReady=false` 提示改用 JSON）；dev@5174 由编排器管、本会话未重启，vue-tsc 绿但已在交付注明需重启预构建。
<!-- 追加模板：
### L-NNN <一句话现象>
- 范围/模块：
- 根因：
- 解法：
- 防复发：
- 关联：
-->
